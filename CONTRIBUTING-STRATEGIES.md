# 贡献策略到 QuantStudio

感谢你愿意把自己的策略分享出来。本文档说明**贡献前必须知情的事**、贡献流程、
以及我们对提交内容的审查方式。

---

## 一、贡献前请先确认这三件事（重要，请勿跳过）

### 1. 你的策略将**全球公开可见**

策略贡献的目标仓库是 **公开仓库** `github.com/yangyizhu8/quantstudio`。你提交的策略
一经合入，**任何人**都可以在互联网上查看、下载这份代码。

**请不要提交**包含以下内容的策略：

- 你的真实姓名、身份证号、手机号、住址、券商账号等**个人信息**；
- 你的**实盘资金规模、持仓明细、成交记录**等私密交易信息；
- 通过付费渠道获得、且**不允许再分发**的数据或算法；
- 你所在机构视为**商业秘密**的策略逻辑。

> 建议：把策略中所有私密信息（资金量、账号、具体标的偏好）改为通用参数。

### 2. 你的策略将**随 QuantStudio 客户端分发至全体客户**

合入的策略**不只是在 GitHub 上公开**——它会被打包进 **QuantStudio 客户端分发包**
（`quantstudio-plus`），随产品一起交付给**本项目的全体客户**。

也就是说，你的贡献会被**商业客户**下载使用。请确认你对此**知情并同意**。

### 3. 首次贡献时，自动化检查**需要维护者手动批准**才会运行

这是 GitHub 对**公开仓库首次贡献者**的保护机制：你提交 PR 后，仓库维护者需要点一次
「Approve and run workflows」，自动化检查才会开始执行。

**所以：你看到检查长时间「等待中」是正常的，不是你的代码有问题**，也不是维护者无视你。
维护者通常会在一天内处理。

---

## 二、贡献流程

本项目采用 **fork + Pull Request**（你**不需要**仓库的写权限）：

1. **Fork**：在 GitHub 上打开 `github.com/yangyizhu8/quantstudio`，点右上角 **Fork**，
   得到你自己的副本（例如 `github.com/<你的用户名>/quantstudio`）；

2. **克隆你的副本**（本地）：

   ```bash
   git clone git@github.com:<你的用户名>/quantstudio.git
   cd quantstudio
   ```

3. **新建分支并放入策略**：

   ```bash
   git checkout -b strategy/<你的策略名>
   # 把策略 .py 文件放到：quantstudio/backtest/strategies/
   cp /你的路径/我的策略.py quantstudio/backtest/strategies/
   git add quantstudio/backtest/strategies/我的策略.py
   git commit -m "feat(strategy): 新增本地策略「我的策略」"
   ```

4. **推送并提 PR**：

   ```bash
   git push origin strategy/<你的策略名>
   ```

   然后在 GitHub 上按提示创建 Pull Request，目标分支选 **`main`**。

---

## 三、可以改什么

**✅ 只允许修改这一个目录：**

```
quantstudio/backtest/strategies/     （且仅 .py 与 .md 文件）
```

**❌ 不允许修改任何其他文件**，包括但不限于：

- `quantstudio/backtest/` 下的其他模块（引擎、取数 API、数据访问层等）；
- `scripts/`、`tests/`、`config/`、`docs/`；
- 仓库根目录的任何文件（包括 README、依赖清单、工作流配置）；
- **修改工作流文件（`.github/`）尤其严格禁止**——那等于修改审查规则本身。

> 自动化检查会**拦截任何越界改动**，且会检查**改名跨越目录边界**、**符号链接**、
> **子模块**等绕过手法。越界的 PR 无法合入。

**为什么这么严？** 因为框架代码是全体客户共用的——一处框架改动会影响所有人的回测
结果与实盘行为。策略贡献只应包含策略本身。

---

## 四、策略文件元信息头（必需）

**新增**的策略文件必须在**文件头的文档字符串（docstring）**里包含以下四项元信息，
否则自动化检查会拒绝：

```python
"""我的策略简称.

@strategy_name: 均线动量轮动
@author: <你的 GitHub 用户名>
@license: MIT
@risk_level: 中
@source: 原创 / 改编自 <来源>       （可选）
@description: 一句话说明策略逻辑       （可选）
"""


def initialize(context):
    ...
```

| 字段 | 要求 |
|---|---|
| `@strategy_name` | 策略名称，非空 |
| `@author` | 你的署名（建议用 GitHub 用户名） |
| `@license` | 许可标识，建议 `MIT`（见第五节） |
| `@risk_level` | **低 / 中 / 高** 三选一 |
| `@source` | 可选。原创就写「原创」；改编或参考他人成果**请如实标注来源** |
| `@description` | 可选。一句话说明逻辑，便于其他用户理解 |

---

## 五、许可与免责

### 许可：MIT（本项目策略贡献的**正式条款**）

本项目要求策略贡献以 **MIT 许可** 提供。请在策略文件头写明：

```python
@license: MIT
```

**MIT 许可允许**：任何人免费使用、修改、分发、商用你的代码。

**MIT 许可要求**：保留你的版权声明与许可声明。

> 若你有其他许可要求（如禁止商用），请在 `@license` 中写明并在 PR 描述中说明——
> 但请注意：**限制性许可将导致你的策略无法随客户端分发给客户**（客户使用场景包含
> 商业用途），从而被**推迟或不予合入**。

### 免责声明（**正式条款**）

**任何策略仅供学习与研究使用，不构成投资建议。** 历史回测表现不代表未来收益。
使用者须自行承担全部交易风险。贡献者不对任何使用后果负责。

### 你保留什么

- 你**仍然是**你策略的著作权人；
- 合入不代表你放弃任何权利，也不代表本项目对你负有义务；
- 你可以随时要求撤下你的策略（开 Issue 告知即可）。

> 本节自 **2026-09-19** 起适用于新增贡献；对已合入的贡献不溯及既往。

---

## 六、提交后会发生什么（四道审查）

| # | 环节 | 说明 |
|---|---|---|
| 1 | **路径门**（自动） | 检查是否只改了 `quantstudio/backtest/strategies/`，且无符号链接/子模块 |
| 2 | **静态扫描**（自动） | 检查是否含禁用项：网络访问、启动进程、动态执行（`eval`/`exec`）、文件读写、内部模块引用等 |
| 3 | **本地沙箱试跑**（维护者执行） | 在**隔离数据副本**上短窗跑一次，确认能正常跑完、无异常、无超时 |
| 4 | **人工审查**（维护者） | 审阅策略逻辑：是否存在**未来函数/前视偏差**、计算是否正确、风险控制是否合理 |

**通过第 1、2 道门不代表安全结论**——静态检查只能过滤明显问题，真正的把关在第 3、4 道。

**维护者可能提出的常见修改意见：**

- 策略里用了回测当日之后的收盘价（**前视偏差**）——这是最常见的退回原因；
- 硬编码了具体标的或日期，建议改为参数；
- 缺少必要注释，不易理解；
- 元信息头不完整。

---

## 七、常见问题

**Q：我的策略能在实盘用吗？**
A：合入的策略**不会自动进入实盘**。实盘策略需经过本项目独立的集成与验证流程。
公开的策略仅作为学习与回测参考。

**Q：我可以提交带外部依赖的策略吗？**
A：不行。策略须为**零外部依赖**（只用回测框架提供的 API 与 Python 标准计算库），
否则客户环境无法运行。特别地，**禁止在策略中直接读取数据库、文件或访问网络**。

**Q：我的 PR 一直没被处理？**
A：参见第一.3 节——首次贡献者需维护者手动批准运行检查。若超过一周无响应，
可以在 PR 下礼貌留言。

**Q：我可以提交别人的策略吗？**
A：**不可以**，除非你已获得原作者授权且该授权允许再分发与商用。若为改编，
请如实填写 `@source` 并确认原许可允许。**侵权内容一经发现立即撤下。**

**Q：提交后我的策略被改动了怎么办？**
A：维护者可能为符合规范而做小幅调整（如删除硬编码、补注释）。实质性改动会在 PR 中
与你沟通，不会在你不知情的情况下改写你的策略逻辑。

---

## 八、有问题找谁

在仓库开 **Issue** 提问即可。提交 PR 前若不确定，也可以先开 Issue 说明你的想法，
我们会告知是否符合收录标准。
