{
  "type": "duckdb",
  "path": "data/quantstudio.db",
  "_note": "MCP模式采集与回测统一使用正式库 data/quantstudio.db（upsert幂等，MCP数据与旧源数据不冲突）。P3验证期间曾用staging库隔离，验证通过后回归正式库。",
  "quarantine": {
    "path": "data/quarantine.db",
    "retention_days": 7,
    "auto_archive": true,
    "max_rows": 500000,
    "rate_alert_threshold": 1000,
    "_note": "与默认config共用同一隔离区（同库，不分离）"
  }
}
