# R2.5 确认包 · 股息防守小市值五日轮动

- 对应设计：`agent_workspace/dividend_defense_smallcap_5d/agent_strategy_design.json`（design 2.3，schema **VALID**）
- 对应策略名（发布文件名）：**股息防守小市值五日轮动**（strategy_id `dividend_defense_smallcap_5d`）
- 生成目标：本地 QuantStudio 单目标（不产出 PTrade）；R5 回测由 agent 托管执行
- 闸门：**以下 A-1~A-9 与三项 evidence 键全部 confirmed=true 后，才进入 R3 生成**（skill 绝对规则 4，一行码不先落）

> 回填格式：每条请你给一句**原话**即可。我会以 `source=customer_reply` + timezone-aware ISO 时间戳写入 `confirmation_evidence`。

---

## 一、近似项 A-1 ~ A-9（逐条人话 + 影响）

| # | 人话解释 | 影响 / 为什么需要你点头 |
|---|---|---|
| **A-1** | **「上市满 1 年」的基准日，本地拿到的是「该股在库里的第一根日 K 线」，不是交易所的真实上市日。** 例：茅台的库内基准是 2018-01-02，真实上市日是 2001-08-27。 | 回测窗口内新上市的 1,971 只里，**只有 1 只**（301618）差 8 天；2018 年前上市的老股两种口径都判「满 1 年」，**选股池不受影响**。属口径瑕疵，不是逻辑改动。 |
| **A-2** | **股息率的「近 12 个月」按每股现金分红逐笔累加，不做送转股的历史回溯调整。** | 与 PTrade 平台 `bonus_ps` 同口径，本地与平台一致；缺点是送转股当期每股指标横向不可比（学界常见做法，不影响排序方向）。 |
| **A-3** | **估值快照的时点锚在 T-1（上一交易日），不是当天。** 这是本地实测确认的既有行为，不是我改的。 | 本策略两类读取分走两条路：**T-1 估值**（流通市值、当日换手率）与锚点重合 → 走**快照**路径；**更早日期**（近 20 个交易日换手率序列的 T-20…T-2）→ 走 **B2 修复后的真 as-of** 路径。两端语义均经 B2 修复与验收定谳。 |
| **A-4** | **成本口径**：双边佣金万三、最低 5 元；印花税与过户费按框架默认；比例滑点 0.001。 | 与你 R0 采纳的方案一致；影响回测收益的绝对值，不影响选股。 |
| **A-5** | **涨跌停与停牌只做执行层处理**：涨停买不进 → 跳过并留现金（不递补）；跌停卖不出 → 顺延到下一个能卖的日子；换出遇停牌 → 保留卖出尝试、不静默放弃。**三项都不进入选股与排序。** | 这是「不额外新增因子」约束的执行层边界。**若你希望改为递补或另有处理，请明说**（会改变持仓，需重新确认）。 |
| **A-6** | **历史估值查询返回的行的顺序不保证稳定**，所以代码里一律**按代码/日期对齐取值**，绝不按第几行取。 | 纯实现纪律，不改变任何结果；写进设计是为了防止未来维护时踩坑。 |
| **A-7** | **股息数据接口是「按某一天精确查」的**，算近 12 个月必须一天一天扫。实测单次约 1.4–1.55 毫秒。 | **性能契约**：必须做「游标增量 + 事件缓存」。不做缓存全回测约 **114 分钟**，做了约 **16 分钟**。R5 主跑我会**提前声明预期墙钟**，避免看起来像卡死。 |
| **A-8** | **持仓字段只用六个白名单字段**（`sid` / `amount` / `enable_amount` / `cost_basis` / `last_sale_price` / `market_value`）。 | 起因是发现某个策略读了一个**在所有已知持仓对象上都不存在**的字段 `.value`。该字段在平台侧是否存在尚未定谳（已排入平台探针批次）。**白名单把不确定性挡在框架线内**，本策略不受影响。 |
| **A-9** | **如果二次筛选后候选不足 5 只** → 按**实际数量**等权、不递补、把可投的钱用完。（例：只剩 3 只 → 每只约 32.3%） | 你的原始规则只写了「保留波动率最小 5 只」，**没有定义不足 5 只怎么办**。另外单票 20% 上限是「5 只等权 = 19.4%」的推论值，**不是你的独立约束**；不足 5 只时实际权重会超 20%，此时按实际权重放行并在审计行标记。**这条需要你明确点头。** |

---

## 二、三项 confirmation_evidence 键（需你给原话）

| 键 | 人话解释 | 需要你说的 |
|---|---|---|
| **r5_deployment_invariants** | **回测期间每期调仓，我按「实际持仓 5 只、总敞口 ≈97%、先卖后买」核账**，并把每期结果写成机器可读审计行。**目标现金约 3%**（缓冲带，用于费用/整手取整/微小漂移）；**若因涨停或停牌建不满 5 只，现金允许临时更高（上限 35%）**，并在审计行标记合法原因。 | 请确认这套「每期核账口径」符合你的预期 |
| **execution_approximations** | 上面 **A-1 ~ A-9 全部九条**的总体确认 | 一句「A-1~A-9 确认」或逐条指出异议 |
| **component_plan** | 策略骨架：生命周期钩子（initialize / handle_data / after_trading_end，**无 before_trading_start**）、所需 API 清单（**13 个**，全部已注册在案）、以及实现注意（性能契约 / 持仓白名单 / PIT 纪律 / 参数冻结） | 确认骨架可以照此落地 |

---

## 三、已确认事项（无需再答，仅备查）

- 生成目标：本地 QuantStudio 单目标 + agent 托管 R5（甲）
- 策略语义：R0 01/02/03/04/05/06/08 确认 + 07 改为 runtime_total_value + 09 成本采纳（丙）
- 资金契约：单只 = 运行时总资产 ×(1−3%)÷5 ≈ 19,400 元；gross 0.97；buffer 0.03（丙 07）
- 调仓融资：close 模式、先卖后买、同日资金可用
- 回测窗口：样本内 2020-01-01~2024-12-31 / 样本外 2025-01-01~2026-07-31（000300 水位 R5 前复核）
- 策略名：股息防守小市值五日轮动（丁）

---

## 四、本轮设计审计的 2 项必修（已修，供复核）

1. **持仓分母自相矛盾** —— 原 `entry_rules` 写死「÷5」，而 `portfolio_contract.allocation_denominator` 是 `actual_selected_count`，候选 <5 时两者分叉。**已统一为 actual_selected_count 语义**（单只 = 总资产 ×0.97 ÷ N），并新增 **A-9** 定义边缘语义；`max_single_weight=0.2` 已注明为**设计保守推论、非客户独立约束**。
2. **`confirmation_evidence` 五键缺口** —— 规则 22 要求五键 verbatim。实测 schema **不允许未确认占位**（confirmed 必须为 true 且 customer_text 非空），故不在设计里塞占位键，改为在 `r2_5_confirmation_pending` 显式登记待收取清单，**R2.5 回填**。

## 五、备注项（审计提出，已接受）

- 股息公式未写 `div_proc` 兜底：R1 已证全表 54,023 行皆为「实施」、无区分力，兜底不可达 → 接受省略
- `decision_events` 的 "14:55" 在 daily-bar-v1 下为记号性标签（handle_data 每日一次、close 撮合）→ 与恐慌抄底先例一致，保持
- 执行层边界第三项「停牌防御性卖出通道」在本策略（无止损）下**只服务「换出卖出遇停牌」**场景 → R3 按此语义落
