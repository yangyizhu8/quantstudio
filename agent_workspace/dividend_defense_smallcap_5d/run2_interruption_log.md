# 异常登记：run2 被机器重启中断（作废重跑）

- 时刻：2026-09-13（日志末行 01:30:06 = 重启时刻）
- 判定依据（三条，缺一不可）：
  1. `Post-close` 仅 **90 行**（全窗口应为 1,594 行）；
  2. **无完成标记**（无 `completed` / `result exported`）；
  3. **快照目录未生成**（`run_r5.py` 未跑到快照步骤）。
- 处置：**run2 产物作废，重跑**（`run2b`）；中断日志归档 `evidence_run2_interrupted/run2.interrupted.log`（78,236 bytes，留证不删）。
- 定性：**环境中断，非策略/引擎异常** —— 中断前 90 个交易日 **空池日 0 / 有持仓日 90 / 每日 positions_total=5**，
  再次印证 B 分支补数据生效（补数据前同期为空池）。
- G3.5 配对：需 **两跑完整产物**（run2b + run3），三件套 SHA-256 逐位一致才作主跑。
