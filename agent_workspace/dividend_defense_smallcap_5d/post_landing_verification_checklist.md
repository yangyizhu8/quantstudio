# 数据落地后 · 起跑 R5 前的核验清单（含回填前基线指纹）

## 零、回填前基线（**已抢在数据线写入前捕获**）

- 捕获时刻：**2026-09-13T00:16:58.725137+08:00**（数据线回执 00:12 接收、即刻起跑后 5 分钟内，写入前）
- 数据源：`D:\miniQMT策略实盘\QuantStudio\data\quantstudio.db`（只读连接）
- 落盘：`pre_backfill_baseline.json`

| 项 | 值 |
|---|---|
| 行数 | **128,764** |
| **业务字段指纹**（15 列：code/end_date/ann_date/…/basic_eps，**不含 update_time**） | `1186810097110654605595247` |
| **全字段指纹**（业务字段 + update_time + data_source） | `1189233607408065504705746` |
| 年份分布 | 2018:13,271 / 2019:13,550 / 2020:14,022 / 2021:15,366 / 2022:16,453 / 2023:17,598 / 2024:17,827 / 2025:18,117 / 2026:2,560 |
| data_source | 全部 `mcp`（128,764） |

> 指纹算法：`SUM(CAST(hash(concat_ws('|', COALESCE(CAST(col AS VARCHAR),'~') ...)) AS HUGEINT))`，
> **与行序无关**（SUM 交换律），对内容逐值敏感。

## 一、核验清单（数据落地后逐条执行，结果入验收记录）

| # | 核验项 | 判据 | 结论位 |
|---|---|---|---|
| **①** | **覆盖到位** | `year(epoch_ms(end_date))` 含 **2015 / 2016 / 2017** 年度行（12 月） | ✅ **通过** |
| **②** | **存量行零衰减（V4 对账口径 · 审计新增）** | 回填后重算**业务字段指纹**必须 == 基线 `1186810097110654605595247`；且 `全表行数 == 128,764 + 新增行数`，**不允许任何既有行被改写或删除** | ✅ **通过** |
| ②附 | 全字段指纹若变化 | 归因（`update_time` 刷新属可接受；**业务字段变化则直接判不通过**） | ✅ 已归因 |
| **③** | **本线代码零变更确认** | 设计 JSON / `strategy.py` / R4 哈希均零变更（B 分支承诺） | ⏳ 待策略线自证 |

### ① 实测（数据线，2026-09-13 00:3x）

```
year(epoch_ms(end_date)) -> rows / codes        （年报行 = end_date 在 12 月）
  2015   10,462 / 3,403      年报 3,085   <== 本次补入
  2016   11,953 / 4,120      年报 3,599   <== 本次补入
  2017   12,639 / 4,389      年报 4,146   <== 本次补入
  2018   13,271 / 4,318      年报 3,973   （既有）
  …2025  18,117 / 5,208      年报 5,204   （既有）
  2026    2,560 / 2,560      年报 —       （季报/中报，无 12 月行）
```

- `end_date` 范围：**2015-03-31 ~ 2026-03-31（北京时间读法，本库约定）**
- 2026 年 2,560 行均为季报/中报，**不影响年度判据**（如 §一风险保留所述）

### ② 实测（业务字段指纹逐位一致）

| 项 | 值 |
|---|---|
| 基线业务字段指纹（回填前，15 列） | `1186810097110654605595247` |
| **回填后 FY2018+ 子集业务字段指纹** | **`1186810097110654605595247`** ✅ **逐位一致** |
| 算法口径复现校验（对写前备份库 `quantstudio_backup_20260912.db` 复算） | `1186810097110654605595247` ✅ **同值** → 指纹可比 |
| 全表行数恒等式 | **128,764 + 35,054 = 163,818** ✅ |
| FY2018+ 行数 | **128,764**（与写前一致）✅ |
| 逐值 JOIN 对拍（13 业务字段，FULL OUTER JOIN） | 参与 128,764 行，**不一致 = 0**；仅新库有 = 0；仅旧库有（被删）= 0 ✅ |

**②附 归因（全字段指纹）**：基线 `1189233607408065504705746` → 回填后 FY2018+ `1185636887712188431561709`，差异**全部来自 `update_time`**（128,764 行全量刷新为本次写入时刻，upsert 语义）；`data_source` 变更 = 0（全 `mcp`）。

> **口径声明（按审核裁定补录）**
> - **对拍指纹 = 13 个业务字段 + `data_source`（未变）**
> - **`update_time` 排除**（upsert 记账字段，全量 128,764 行刷新为本次写入时刻）—— 回写旧时间戳会使审计轨迹失真，故**接受重写、不回写**
> - 因此「零衰减」的精确含义为：**业务字段逐值不变**（13/13 零差异 + `data_source` 不变），策略结果只依赖业务值

> **旁证观察（供审计参考，不阻塞）**：写前备份库（mtime 09-12 00:55）的业务字段指纹 == 基线 ✅，但其**全字段**指纹（`1186652452088879286342690`）≠ 基线全字段指纹 → 说明 `update_time` 在 **09-12 00:55 → 09-13 00:16** 之间已发生过变化（非本线写入：本线首次触碰主库为 09-13 00:27）。结论不受影响，仅提示该表在此期间被其他过程触碰过。

### ①的风险保留：2026 年行为季报/中报

2026 年 2,560 行均为季报/中报（无 12 月年报），**不影响年度判据**；回执出证时建议注明。

## 二、核验通过后本线动作

1. 起 **G3.5 双跑**（原窗口 2020-01-01 ~ 2026-07-31，配置不变：init_cash 100,000 / 000300.SS / daily-bar-v1 close / 主库）
   → 三件套 + 日志全量落盘并哈希绑定 → 两跑 SHA-256 逐位一致才作主跑；
2. **样本内/样本外分段报告**独立呈现；
3. **R5.5**（门控启用、无豁免）：WF 5 折 + MC n=1000 + G1-G6 独立呈现，INSUFFICIENT_SAMPLES 如实标注；
4. 报送 R5 + R5.5 结果；PASS 后 **R6 发布** `quantstudio/backtest/strategies/股息防守小市值五日轮动.py`
   → 用户确认 → 双仓库推送 + QuantStudio-trading 同步门。

## 三、墙钟预声明（沿用）

| 阶段 | 预期 |
|---|---|
| G3.5 双跑 | 每跑约 16 分钟量级（股息游标缓存生效） |
| R5.5 五折重扫 | 约 80+ 分钟 |
| 数据落地后核验（本清单 §一） | 秒级 ~ 分钟级 |
