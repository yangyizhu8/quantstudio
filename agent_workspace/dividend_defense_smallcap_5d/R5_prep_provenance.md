# R5 预备文档：provenance 补记 + 指纹口径声明

## 一、income_statement FY2015–FY2017 补拉（客户裁定 B）

| 项 | 内容 |
|---|---|
| 客户裁定 | **B**（原文：「B」）——补拉 FY2015–2017 年报，保住原 5 年样本内窗口 |
| 派单 | `B_dispatch_request.md` / `B_dispatch_prompt.md`（策略生成线 → 数据拉取线，经审核会话核准排期后下发） |
| 回执 | 数据线 2026-09-13 00:12 接收、即刻起跑；载具 = 既有 daemon `--mode once --pull-mode full_range --quality-audit full`，profile `prehandover_staging`，只动 `income_statement` |
| 数据线证据 commit | **`ea91dc7`**（数据线交付证据；本会话复核引用） |
| 完工时刻 | 2026-09-13（当日；ETA 40–90 分钟窗口内） |

### 落地核验结果（本会话独立复核，4/4 PASS）

| # | 核验项 | 结果 |
|---|---|---|
| ① | 覆盖含 2015 / 2016 / 2017 | **PASS** — 2015:10,462 ／ 2016:11,953 ／ 2017:12,639 行 |
| ② | `MIN(end_date)` 北京读法 | **2015-03-31**（UTC 读法 2015-03-30）——与审计期望值一致 |
| ③ | 行数守恒 | **PASS** — 总行 163,818 = 基线 128,764 + 新增 35,054 |
| ④ | **存量行零衰减** | **PASS** — 存量集合（年份 ≥2018）行数 128,764、**业务字段指纹 = 基线 `1186810097110654605595247` 逐位一致** |

→ **B 分支对本线关单**。

## 二、指纹口径声明（update_time 裁定配套）

**对拍指纹口径（零衰减判据的精确边界）**：

- **纳入**：13–15 个**业务字段**（`code`/`end_date`/`ann_date`/`operating_revenue`/…/`basic_eps`）+ `data_source`（实测未变，全为 `mcp`）；
- **排除**：**`update_time`** —— upsert 记账字段，**非业务数据**；本次回填全量 128,764 行刷新为本次写入时刻，
  属 **upsert 语义的预期结果**，**不构成零衰减违背**；
- **裁定依据**（审核会话 2026-09-13）：① `update_time` 为记账字段，回写旧时间戳反而使审计轨迹失真（该行确实被本次 upsert 触碰）；
  ② 零衰减契约的实质是**业务字段逐值不变**（与策略结果只依赖业务值一致）；③ 回写需再动一次全表，风险 > 收益。
- **基线指纹**：`pre_backfill_baseline.json`（捕获于 2026-09-13T00:16:58+08:00，**抢在数据线写入前**）
  —— 业务字段指纹 `1186810097110654605595247`；全字段指纹 `1189233607408065504705746`（仅作参照，允许因 `update_time` 变化）。

## 三、本线代码零变更确认

| 项 | 状态 |
|---|---|
| 设计 JSON | **零变更**（B 分支不改窗口；无 A-10；无 R4 复跑） |
| `strategy.py` SHA-256 | `25C082CF2C7780BF9031D2050FBEDF26520DB775FF6086E815583C26815D7850`（23,030 B，与 R4 时一致） |
| R4 静态校验 | 仍为 **PASS / 0 block / 1 WARN**（无需复跑） |
| 等价性硬测 | **不重跑**（两次记录已齐；B 分支与股息逻辑无关） |

## 四、G3.5 与后续

- **RUN1（2020-01-01~2026-07-31，补数据前）**：已归档为**空池期量化证据**（1,594 交易日 / 空池 500 日 / 31.4%），
  **不作为 G3.5 配对跑**（数据已变，哈希必然不同）。
- **G3.5 双跑**：run2 与 run3（**均为补数据后、原窗口、配置零变更**）→ 三件套 + 日志哈希绑定 → 两跑 SHA-256 逐位一致才作主跑。
- 随后：样本内/样本外分段报告 → **R5.5**（门控启用、无豁免）→ 报送 R5 + R5.5 → R6 发布。

## 五、墙钟预声明

| 阶段 | 预期 |
|---|---|
| G3.5 每跑 | 约 16 分钟量级 |
| R5.5 五折重扫 | 约 80+ 分钟 |
