{
  "type": "duckdb",
  "path": "data/quantstudio.db",
  "quarantine": {
    "path": "data/quarantine.db",
    "retention_days": 7,
    "auto_archive": true,
    "max_rows": 500000,
    "rate_alert_threshold": 1000,
    "_note": "防膨胀：7天归档、50万行硬上限、单规则单日1000行速率提示。path 用相对项目根路径，由 _paths.get_data_root() 锚定到项目根解析（不依赖 cwd，daemon 跨目录运行正确）。客户移交时数据库解压到 <项目根>/data/quantstudio.db。"
  }
}
