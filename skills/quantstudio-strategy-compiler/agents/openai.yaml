interface:
  display_name: "QuantStudio Strict Agent-first Strategy Engineering"
  short_description: "Strict R0-R6 with agent/user-PyQt backtest ownership"
  default_prompt: "Use $quantstudio-strategy-compiler. Execute R0-R6 strictly. At R0 separately confirm target platforms and whether R5 is agent-managed or user-PyQt. User-PyQt mode creates only a hash-bound __candidate QuantStudio file after R4, waits for complete PyQt logs/report, routes failures to R1/R3/R4, and unlocks R6 only after evidence PASS. Formal promotion regenerates target files and removes the candidate. Never hardcode or privately choose backtest dates."
