meta:
  wrapper_hash: d1c47919cf69
  last_reverified: '2026-08-31'
  probe_refs:
    P1: date-only/range 多期形态 + publ_date PIT 9/12（probe-v21b-platform-evidence.md）
    P2: 3 表×8 净资产/股本字段 EMPTY（种子缺口）
    P3: profit_ability.roe date 模式 OK / 无 fin_indicator
    P4: get_industry_stocks 480000.XBHS=42 银行池
    P5: 待运行：probe-p5-matrix-20260831.py 已落盘 docs/evidence/（list+date+range 小池 / roe
      list / eps / or_yoy / report_types×range）；P5-7 dist={0:3,1:6,2:20,3:35,4:47,5:59,6:45,7:21,8:13}
      pass6=79 cost_missing=0 cur_missing=0（RD-4 定量证据）；v8.7b 九次复验收敛：fscore_pass 35、roa_removed
      15、total_share 0——RD-5 定量证据
matrix:
- id: f01
  shape: single
  mode: date-only
  table: income_statement
  fields:
  - np_parent_company_owners
  - operating_revenue
  - operating_cost
  - end_date
  tested: true
  probed: true
  probe_ref: P1
  rd: null
  notes: single+date-only 基线；逐股退化路径
- id: f02
  shape: single
  mode: date+range
  table: income_statement
  fields:
  - np_parent_company_owners
  - operating_revenue
  - operating_cost
  - end_date
  tested: true
  probed: true
  probe_ref: v8.7b 九次复验（2026-08-31）：B6c range 全量执行 QS_GF_CALL n=200×3表；fscore_pass
    166→35；roa_removed 0→15（同比真命中）
  rd: null
  notes: B6c 主路径收敛：range 多期+mul2 拍平+PIT 值域兜底+end_date epoch 毫秒契约（v8.7）；fscore 数值收敛
    35（数据源差见 RD-5）
- id: f03
  shape: single
  mode: date-only
  table: balance_statement
  fields:
  - total_assets
  - total_liability
  - total_current_assets
  - total_current_liability
  - end_date
  tested: true
  probed: true
  probe_ref: P1
  rd: null
  notes: ''
- id: f04
  shape: single
  mode: date-only
  table: valuation
  fields:
  - total_share
  tested: true
  probed: true
  probe_ref: P2
  rd: RD-1
  notes: 8 字段族 EMPTY → seeds 首调短路 NaN 行；⑦ 恒不加分（RD-1 固化断言）
- id: f05
  shape: single
  mode: date-only
  table: profit_ability
  fields:
  - roe
  tested: true
  probed: true
  probe_ref: P3
  rd: RD-2
  notes: date 模式非 PIT（无 ann_date）；仅 Step5 排序消费（RD-2 固化断言）；roe 列表批量 probe:P5
- id: f06
  shape: single
  mode: date-only
  table: growth_ability
  fields:
  - or_yoy
  - publ_date
  - end_date
  tested: true
  probed: true
  probe_ref: P1
  rd: null
  notes: or_yoy→operating_revenue_grow_rate 请求翻译 + 返回逆翻译；PIT 前置保留 publ_date（weekly/周频形态）
- id: f07
  shape: single
  mode: date-only
  table: eps
  fields:
  - eps
  - publ_date
  - end_date
  tested: true
  probed: true
  probe_ref: 探针乙（2026-08-24 fidelity，平台 basic_eps/diluted_eps 列存在）
  rd: null
  notes: P-A2：basis=basic → eps→basic_eps 请求翻译 + 返回逆翻译（CANSLIM/weekly/周频形态）；P5-3 平台
    basic_eps/diluted_eps 列存在实证
- id: f08
  shape: batch-single
  mode: date-only
  table: '*'
  fields:
  - float_value
  tested: true
  probed: true
  probe_ref: P1
  rd: null
  notes: get_fundamentals_batch 委托 list 模式（P-D10 实证 500 码 0.05s）；weekly/周频形态
- id: f09
  shape: list
  mode: date+range
  table: income_statement
  fields:
  - np_parent_company_owners
  - end_date
  tested: true
  probed: true
  probe_ref: P5-1（2026-08-31）
  rd: null
  notes: list+range 小池：P5-1 平台实证（18 行 MultiIndex 6 期）+ test_fm_list_range_small_pool
    契约补证 → 缺口闭合
- id: f10
  shape: list
  mode: date-only
  table: valuation
  fields:
  - float_value
  tested: true
  probed: true
  probe_ref: P1
  rd: null
  notes: list+date-only（P-D10 500 码）
- id: f11
  shape: prefetch-pool
  mode: date-only
  table: profit_ability
  fields:
  - roe
  tested: true
  probed: true
  probe_ref: v8.4 平台实证（QS_GF_PREFETCH_SKIP pool=300 幂等 1 行）
  rd: null
  notes: pool>32 → QS_GF_PREFETCH_SKIP（v8.3 大池卡死保守化，2026-08-31；P5 若证大池 list 可用留作放宽依据，本轮不动）
- id: f12
  shape: single
  mode: date+range+report_types
  table: income_statement
  fields:
  - np_parent_company_owners
  - end_date
  tested: true
  probed: true
  probe_ref: P1
  rd: null
  notes: report_types 1/2/3/4 → 0331/0630/0930/1231 过滤（P1 实证）
- id: f13
  shape: industry
  mode: pool
  table: get_industry_stocks
  fields: []
  tested: true
  probed: true
  probe_ref: P4
  rd: RD-3
  notes: 480000.XBHS+490000.XBHS 并集 121；无效码（801780 等）fail-open → 哨兵 '999999'（RD-3
    固化断言）
- id: f14
  shape: single
  mode: empty-return
  table: '*'
  fields: []
  tested: true
  probed: true
  probe_ref: P2
  rd: null
  notes: 平台空返回 → 1 行 NaN 契约（P-D10 v1.2）；策略 KeyError 免疫
- id: f15
  shape: single
  mode: date+range-pit
  table: income_statement
  fields:
  - np_parent_company_owners
  - publ_date
  - end_date
  tested: true
  probed: true
  probe_ref: P5-1/P5-7（2026-08-31）
  rd: RD-4
  notes: 平台 list+range 模式 publ_date 全空（P5-1 empty=18）→ PIT 空串须容错；未披露占位期（2026-06-30
    值全 NaN）由值域兜底剔除——v8.6 修复（v8.5 空串 ValueError 整表放行 → NaN 期成 cur → fscore 实跑 3 vs
    探针复算 79）
- id: f16
  shape: data-source
  mode: diff
  table: '*'
  fields: []
  tested: true
  probed: true
  probe_ref: v8.7b 九次复验 + P5-7（2026-08-31）
  rd: RD-5
  notes: 平台 get_fundamentals 财务数值与本地 provider 数据源差（非 wrapper 可修）：同 F-Score 判定下 fscore_pass
    35（平台）vs 97（本地）；P5-7 平台数据复算 79（判定实现差）；rv_removed 6 vs 50（行情 RV 口径差）；候选 8 只均为平台真绩优（ROE
    4~11.6）
