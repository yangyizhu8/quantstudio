# §21.4b ROE 覆盖采数·结案归档（2026-09-06，任务三乙结案）

- 采数载体：`roe_probe_batch`（多码批量 profit_ability+roe，12 码 = 10 缺失候选 + 2 正常对照）
- 平台运行：2026-09-06，窗口 2026-01-01~2026-04-30（缺失码集中期），日期=20260131
- 裁定：**乙结案**（附两处口径修正 + 重开触发条件）

## 1. 采数实证表

| 项 | 实测 |
|---|---|
| 批量请求 | 12 码 profit_ability+roe @date=20260131（QS_GF_CALL n=1 secs=12） |
| 平台返回 | 12 行 |
| roe 缺失/NULL | **0**（`_qs_gf_val_missing_any(['roe'])` = False） |
| 探针行为 | 按设计静默（gate 条件不成立 → 内部分支不执行，无 QS_ROE_PROBE 行——正确行为） |

## 2. 定谳（两处口径修正版）

1. **"永久缺列"证伪——限定口径**：仅限 **@20260131 单时点批量形态**下，12 候选码
   roe 列存在且全有值。不写无范围的全称否定。
2. **"期间缺失"未复现——假说挂起非定论**：P3<P2 差异（~12 码/1-4 月）在**单时点批量
   形态**下未复现；**两期同比形态（F-Score `_latest_statement` start_year/end_year+
   report_types）的缺失假说未验证**（测它需探针 v3 扩触发条件至多码×多期）——假说与
   事实分开记录。
3. **ROE 派生 mini-方案不立项维持**：当前已测形态下 roe 有值，派生无的放矢。

## 3. 重开触发条件（防真问题溜走）

若平台侧 F-Score 生产/回测运行**再次出现候选码因 ROE 缺失被 P3 剔除**（平台日志/
结果可查）→ 即以**探针 v3**（触发条件扩至多码×多期）立案采数 → 届时若证实期间缺失，
wrapper 派生（ROE=净利/权益）按 mini-方案走六步（ROE 有生产价值：让平台 F-Score
不漏掉这 12 码的可评估机会）。在此之前不为未证实假说支付一次模板变更+哈希周期成本。

## 4. 载体与产物

- 载体源：`quantstudio/backtest/strategies/roe_probe_batch_quantstudio.py`（一次性
  采数工具，采数完成即弃——是否删除由后续清理决策）
- 产物：`output/ptrade_export/roe_probe_batch/roe_probe_batch_ptrade.py`（探针 v2
  注入 10 处，api_portability PASS）
- 平台日志：SG-MS-PEG 对账目录外用户侧回贴（本报告 §1 实测表为准）