# F-Score ①-⑨ 评分项归因表（终稿）—— 35 / 79 / 97 三端拆解

> 日期：2026-08-31｜依据：v8.7b/v8.7c 平台实跑 + P5-7/P5-8 全池复算 + QS_GF_META 逐码期次证据 + 本地引擎复现

## 0. 口径修正（关键前置）

策略 `FSCORE_MIN=6`、`if fs <= 6: continue`（strategies/F-Score选股RSRS择时.py L29/L310）→ **`fscore_pass` = fs>6（≥7）计数**。
探针 P5-7 的 `pass6` = **fs≥6 计数**（含 45 只 fs=6）。**此前"44 差（35 vs 79）"是口径错位（>6 vs ≥6），不成立**。

## 1. 三数修正矩阵

| 端 | 口径 | 数值 | 依据 |
|---|---|---|---|
| 平台实跑（v8.7b/v8.7c 两轮稳定）| fs>6 | **35**（候选 8 只两轮一致）| QS_SIGNAL_AUDIT fscore_pass=35（两轮）；QS_GF_META 全码期次 |
| 探针复算 P5-7（平台原生）| fs≥6 | 79（dist 中位 5；fs=6:45、7:21、8:13）| P5-7 dist |
| 探针 fs>6（同口径）| fs>6 | 34（= 21+13）| P5-8 解析（日志手抄缺 2 行 fs=8 → 解析 32；P5-7 官方 34）|
| 本地引擎复现 | fs>6 | **97**（候选 8 只复现）| 本地引擎 2026-07-01 |

## 2. ①-⑨ 逐项归因（平台 fs>6 集，P5-8 逐码明细节选）

判定实现差审计（本地对照 5 用例）：**4/5 完全一致**（含银行 cost NaN、prev 缺失、负值）；
唯一差异 = cur 全 NaN 边界（策略抛 TypeError；平台真实数据被 PIT 值域兜底剔除、不触发）
→ **判定实现差 = 0（真实数据域）**。

平台 fs>6 集（34 只）逐项通过率（P5-8 解析 32 只样本）：
①净利>0 100% ｜②现金流>0 100% ｜③ROA↑ 97% ｜④现金流>净利 84%
⑤杠杆↓ 91% ｜⑥流动比↑ 84% ｜⑦无增发 0%（RD-1：平台无 total_share，恒 0 已登记）
⑧毛利↑ 88% ｜⑨周转↑ 91%

**顶部落差 = 0**：实跑候选 8 只（002027/000807/601899/002532/600989/601600/002602/600346）
在探针侧全部 s=7/8、d 明细一致 → **wrapper 通路 ≡ 原生通路（top 完全对齐）**。

## 3. 边界差定位（QS_GF_META vs P5-8 全码对比，v8.7c 决定性证据）

- **247/249 码 cur/prev 完全一致**（cur=2026-03-31 prev=2025-03-31）。
- **差异 5 只**：002415/300750/603259/605499（wrapper income cur=2026-03-31 vs 探针合并 cur=2026-06-30）、688981（wrapper prev=2024-12-31 有值 vs 探针 prev 空）。
- **根因 = 探针 P5-8 复算缺陷**：探针按"三表合并行 max(end_date)"取 cur（混期）；
  **本地/策略语义为三表各自取期**（income/balance/cashflow 各 `_latest_statement`，
  与 wrapper QS_GF_META 一致）→ 探针把 2026-06-30 中报期混入 → 复算 79 虚高。
- 4 只特例探针 s=5/6（**均未过 7 线**）→ 不影响 35 真实性。
- **"≤1 只"边界差 = 探针复算实现差（非 wrapper、非数据值）**；35 为策略真实判定。

## 4. 终局归因

| 差异 | 数值 | 归因 | 性质 |
|---|---|---|---|
| 实跑 35 vs 探针 79 | 44 | **口径错位**（>6 vs ≥6）+ 探针混期缺陷 | 不成立/探针缺陷（已注 P5-8 修正注释）|
| 实跑 35 vs 探针 fs>6（34）| ≤1 | 探针混期边界（应用 P5-8 修正后收敛）| 探针复算实现差 |
| 实跑 35 vs 本地 97 | 62 | **平台财务数据源 vs 本地（数据值差）**——本地候选 8 只平台值下全 ≤6（300866 平台 5 分等）反向实证 | **RD-5 定量**（wrapper 不可修）|
| 判定实现差（本地对照）| 0 | 5 用例 4 一致 + 1 边界不触发 | 无 |

## 5. 推送条件判定（用户授权条件：44 差全为数据值差即推送；含实现差先修后推）

- **"44 差"为口径错位，不成立**；修正口径下差 ≤1 只 = **探针复算缺陷**（wrapper≡策略语义，非实现差）。
- **62 差（35 vs 97）全为数据值差**（RD-5，候选维度实证）。
- **无 wrapper/策略实现差成分**（判定 0、top 零差、期次全一致）→ **推送条件达成**。

**结论**：F-Score 平台可移植性验收三轨达成——
轨1 契约断言全绿（161 passed + wrapper 18 分支）；轨2 契约矩阵缺口 0（哈希门 8fda9eb590f1）；
轨3 fscore 数值收敛 35 = 平台数据下策略真实判定（= 本地 97 − 62 数据值差，RD-5）。

## 6. 证据索引

- `probe-p5-matrix-20260831.py`（P5-7 全池复算 + P5-8 逐码明细 + v8.7c 复算修正注释）
- `output/generated_strategies/fscore_rsrs/docs/evidence/probe-p5-8-result-20260831.log`（P5-8 249 码明细）
- v8.7b/v8.7c 平台日志（QS_SIGNAL_AUDIT fscore_pass=35 ×2、QS_GF_META 全码期次）
- 本地引擎复现（候选 8 只 97 基线）