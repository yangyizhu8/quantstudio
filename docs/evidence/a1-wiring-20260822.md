# A1 接线实施与验收证据 — 本地订单 API 拆单包装（2026-08-22 修正）

- 关联：方案 v4 §3.1 A1 + ZCode 复审批准（审核意见 2026-08-22）
- 回退点：`git stash create -u -m "baseline-ptrade-align-ordersplit-wire-<ts>"` → `ba51cfc57c619b91c4d99fb896cae8503de44c51`
- 背景：冒烟发现本地 `order_target_value` 直绑未接线（ptrade_api.py:2241），双端订单序列不一致（本地 5 笔 vs PTrade 10 笔）+ 费用不对称（平台按笔最低佣金）

## 1. 改动内容

| 文件 | 改动 |
|---|---|
| `quantstudio/strategy_compiler/source_import.py` | 转换侧模板补 3 个订单 API 包装：`order_value`（金额语义→拆单链路）、`order_target_percent`/`order_percent`（比例语义→保守回退原 API，平台按市值折算超限风险低）；`_QSOrderRefState` 扩展 5 个 orig 引用 |
| `quantstudio/backtest/ptrade_api.py` | 本地接线：`order_target_value`/`order`/`order_value` 三入口包装（复用 `_qs_split_order`/`_qs_last_close_lookup`）；**注**：本地 `PtradeAPI` 无 `order_target_percent`/`order_percent` 方法（percent 类为平台语义），双端无分叉 |
| `tests/test_ptrade_contract_compliance.py` | +7 用例：本地↔模板逐笔一致、order 股数边界（恰超/非整手）、order_value 链路、px 缺失回退、模板 5 包装、percent 回退、wiring 同构 |

## 2. 费用实证闭环（ZCode 审核修正）

- 本地佣金模型 = `max(成交额×万3.5, 5元/笔)`（backtest_engine.py:230/1064 实证）；
- 平台最低佣金 ≈5 元/笔（探针 20.61/4 笔，a1-commission-probe）；
- **拆单段 per ≤10,000 元 → 两端全部落入最低 5 元 → 对称** ✅；
- 残留风险：未拆单段（如 300854 高价股）平台费率未钉 → **容差 = 单笔佣金差 ≤1 元、净值影响 <0.01%**（ZCode 修正①：按实例 0.52 元放宽）。

## 3. 验收

| 项 | 结果 |
|---|---|
| 单元测试 | ✅ 107 PASS（+7 wiring 用例，含本地↔模板逐笔一致 test_wire_local_vs_template_homology） |
| 转换产物 | ✅ 5 API 包装齐备、白名单风险 ZERO、py_compile 通过 |
| 全库订单 API 扫描 | 4 个历史策略用 order_value（ETF平滑动量/ETF轮动/二八/双均线）——**不在本批推送范围，未重转即不变**（ZCode 修正②：仅重转产物字节级变化，明示非静默） |
| 本地回测对比 | 见 §4（笔数 5→10、佣金、现金分布逐项归因） |
| 冒烟 | PTrade 端 A1 已实证（300029 3 段全成交）；本次冒烟两端选股仅 1/5 重合 → 不作为逐笔一致验收（按 ZCode 口径修正） |

> **测试命令与统计口径**：`python -m pytest tests/test_ptrade_contract_compliance.py tests/test_source_import.py -q` →
> **107 passed**（两套件合计：test_ptrade_contract_compliance.py 67 passed + test_source_import.py 40 passed；
> 其中本批新增 wiring 用例 7 个均在 test_ptrade_contract_compliance.py 内）。ZCode 复核本机
> 67 passed = test_ptrade_contract_compliance.py 单套件口径，两者一致不矛盾。

## 4. 黄金基线对比（回测完成回填）

| 指标 | 接线前（20260822_031532） | 接线后（20260822_041605） | 变化 |
|---|---|---|---|
| 最终净值 | 77,083.16 | 76,220.65 | -862.51（-1.12%） |
| 总收益率 | -22.92% | -23.78% | -0.86% |
| 最大回撤 | 27.69% | 28.70% | +1.01% |
| gross/cash（07-01） | 0.9404 / 5.96% | 0.9824 / 1.76% | 仓位上升 |
| 订单笔数（07-01 买入） | 5 | 5（本策略选股为中高价股未触发拆单） | 见 4.2 |
| buy_submitted / positions | 5 / 5 | 5 / 5 | 不变 |

**归因（逐项）**：
1. 订单笔数：本策略（vol_regime 本地选股 300255/300854/600791/603272/603601，8.35-25.17 元）折算股数 800-2200 股 **<49,000 不触发拆单** → 本地仍 5 笔单笔；PTrade 端选股为仙股（0.15-0.51 元）触发拆单 10 笔。**双端差异根因 = P-D9 选股池差**（已登记），非拆单问题；
2. 佣金增加（本地 5 笔 vs 拆单场景）与净值 -1.12% 归因见 §4.1；
3. **净值 -1.12% 主因 = 买入股数锚定变化（双端对齐的预期行为变更）**：接线前 `order_target_value(20000)` 按**撮合当日价**折算股数；接线后拆单按**统一链 ① 层前收 px** 先定段股数再 `order(amount)`（含整手取整 + 0.95 缓冲截断）→ 买入量与当日折算不同 → gross 0.94→0.98（敞口加大）→ 全月涨跌暴露差异 → 净值差。**PTrade 端自 A1 上线起即为此行为**，本地接线后**双端一致**（本次接线目的达成）。

### 4.1 本地低价股拆单场景验证（A 组推送前置缺口闭环，ZCode 审核要求）

用临时验证策略（直接对 4 只仙股 order_target_value(20000)，复现 PTrade 选股场景）本地回测：

| 验证项 | 结果 |
|---|---|
| 本地拆单分支（n>49,000）真实回测触发 | ✅ 07-01 **buy_filled=10**（000004 2 段 / 002808 3 段 / 002898 2 段 / 300029 3 段） |
| 每笔段股数 ≤49,000 | ✅ 全部 ≤38,000 |
| 最低佣金模型生效 | ✅ 每笔 commission=5.0 |
| 与 PTrade 冒烟段结构对照 | ✅ 同构（10 笔、段数一致；股数因前收 px 取数时点/数据源微差，D3 边缘） |
| 统一链 current_price（① 前收） | ✅ 000004=0.3067 等命中 |
| 资金不足处置对照 | ✅ 本地 `insufficient_cash_or_rounding` 拒单 vs PTrade"调整800股"降量——D4-S4 语义差（已登记），非拆单问题 |

**前置条件：本地拆单链路 + 最低佣金费用模型在真实回测中双实证通过 ✅（缺口闭环）**

### 4.2 笔数/佣金/现金分布逐项归因总结（ZCode 审核要求）

| 维度 | 事实 | 归因 |
|---|---|---|
| 订单笔数 | 本地中高价股 5 笔不拆；仙股场景 10 笔拆 | 拆单正确工作；双端差异=P-D9 池差 |
| 佣金 | 拆单场景每笔最低 5 元（10 笔=50 元 vs 5 笔=25 元） | 两端同构（平台同款最低佣金实证） |
| 现金分布 | 本地 gross 0.98（仙股场景 0.982）/ PTrade 0.994 | 段股数锚定 + D4-S4 处置差异 |

## 5. 后续

- 推送流程：本证据 + 用户确认 → 双仓库推送；
- B 组（退市强平）独立流水线不变。