# GUI 皮肤改造验收证据（GitHub Dark 风 · 对齐参考效果图）

- **日期**：2026-08-23
- **方案**：docs/handoff/gui-skin-baseline-20260823.md（Phase 0 基线）+ 已批准终版方案
- **基线快照**：`git stash create -u` → `32f04b32f3e4377991db53474d2312852b88c2a2`
- **commit 纪律执行**：
  - `84702d9` fix(gui): import MessageBox（D3 前置，独立 commit，先行验证通过）
  - 皮肤改动 **未 commit**，等用户验收确认后另起 `style(gui)` commit，与 84702d9 分列推送

## 一、改动范围（实际落地）

| 文件 | 改动 |
|---|---|
| `quantstudio/gui/skin.py`（新增） | 设计 Token / APP_QSS / 深色 QPalette+Fusion / NAV_QSS / STACKED_QSS / LOG_PANEL_QSS / apply_app_skin / apply_window_skin / PageHeader / colorize_log_line（含 COLOR_LOG_INFO 降级开关） |
| `main_gui.py` | 启动调 `skin.apply_app_skin(app)`（原 setTheme(DARK) 并入） |
| `quantstudio/gui/main_window.py` | 移除 Mica；`__init__` 末尾调 `apply_window_skin`；日志面板卡片化（SYSTEM LOG eyebrow + 运行日志 + 清空按钮保留）；日志信号改接 `_append_log`（appendHtml 着色，信号路径/GuiLogHandler 不变）；D3 的 MessageBox import 已先行独立提交 |
| `tabs/task_tab.py` | PageHeader；`▶ 全部执行`→PrimaryPushButton；两张表 setBorderVisible/Radius（他人未提交改动段 L873-920 未触碰） |
| `tabs/source_tab.py` | PageHeader；_SOURCE_SCROLL_STYLE 换 Token（#0d1117/#e6edf3）；mode_label→ACCENT、hint→WARNING；`💾 保存配置`→PrimaryPushButton |
| `tabs/browser_tab.py` | PageHeader；`▶ 执行`→PrimaryPushButton |
| `tabs/quarantine_tab.py` | PageHeader；表格边框 |
| `tabs/quality_tab.py` | PageHeader；`▶ 执行全部检查`→PrimaryPushButton；表格边框 |
| `tabs/export_tab.py` | PageHeader；`📤 导出`→PrimaryPushButton |
| `tabs/config_editor_tab.py` | 外层包 PageHeader；_DARK_CONFIG_STYLE 换 Token + taskCard 卡片化（#161b22 底/#30363d 边/8px 圆角）；db_unified_label→ACCENT（他人未提交改动段 L71-132 未触碰） |
| `tabs/backtest_tab.py` | PageHeader；`▶ 启动回测`→PrimaryPushButton；D2：按钮行+进度条收进「运行控制 RUN CONTROL」卡片（仅容器调整，信号/逻辑零改动） |
| `tabs/ptrade_export_tab.py` | PageHeader（主按钮原已是 PrimaryPushButton） |
| `backtest_result_window.py` | 167 处颜色字面量按映射表替换（#2b2b2b→#0d1117、#2d2d2d→#161b22、#333/#3a/#35→#1c2128、#383838→#1a2029、#3c3c3c/#404040/#454545/#4a4a4a→#30363d、#e8e8e8→#e6edf3、#a0a0a0/#888888→#8b949e、#808080 网格→#30363d、#0078d7/#007acc→#2f81f7、DWM 标题栏 0x2b2b2b→0x0d1117）+ matplotlib rcParams 调色板；数据语义色（红涨绿跌/橙基准）保留 |
| `kline_viewer.py` | matplotlib rcParams 深色化（原白底与深色主题割裂，修正） |
| `tests/test_gui_dark_panels.py` | 两处颜色断言更新：`#202020/#ffffff` → `#0d1117/#e6edf3`（断言意图"深底+浅字"不变，仅期望值随皮肤更新） |

**功能零改动承诺核对**：全部改动为 QSS/调色板/纯展示部件（PageHeader）/同 API 子类按钮替换（PushButton→PrimaryPushButton）/布局容器收纳；信号连接、Worker、DB、配置解析、任务状态机无一行变更。

## 二、测试验收（口径：与基线对比无新增失败）

运行：`QT_QPA_FONTDIR=C:\Windows\Fonts` + `QT_QPA_PLATFORM=offscreen`，
`pytest tests/test_gui_dark_panels.py --deselect ...::test_backtest_finished_does_not_repeat_completion_log tests/test_gui_rebalance_mode.py tests/test_gui_task_audit_separation.py tests/test_backtest_result_window_savefig.py`

**结果：7 failed, 24 passed, 1 deselected —— 与基线完全一致（PASS 集不变、FAIL 集未扩大）。**

| 项 | 基线 | 皮肤后 |
|---|---|---|
| test_gui_dark_panels（除挂起项） | 4F/7P | 4F/7P（同样 4 条夹具漂移：DummyMainWindow 缺属性/alignment_page 属性缺失） |
| **颜色断言更新验证** | — | `test_config_editor_pages_use_scoped_dark_scroll_panels[sources/tasks]` 按新断言 **PASS**（#0d1117/#e6edf3 生效实证） |
| test_gui_rebalance_mode | 3F/17P* | 3F/17P*（KeyError engine_kwargs，同样漂移） |
| test_gui_task_audit_separation | 全P | 全P |
| test_backtest_result_window_savefig | 全P | **全P**（导图功能回归通过） |

*含与其余两文件合并计数。

## 三、视觉验收（offscreen 截图，before/after 对比）

- before：`output/skin_preview/before/`（12 张，改造前 Mica 灰底）
- after：`output/skin_preview/after/`（12 张：window-full / 9 Tab / with-logs / result-window）
- 生成命令：`python scripts/gui_skin_preview.py --out output/skin_preview/after`
- 采样环境注意：offscreen 需 `QT_QPA_FONTDIR=C:\Windows\Fonts`（否则 CJK 豆腐块）

**像素级验证表（after）**：

| 区域 | 实测 | 目标 | 判定 |
|---|---|---|---|
| 主窗内容底 | `#0d1117` | BG_PAGE | ✅ |
| 侧边栏 | `#0f141a` | BG_SIDEBAR | ✅ |
| 卡片叠层 | `#191d23` | ≈#161b22 家族 | ✅ |
| 结果窗底/图表面板 | `#0d1117` / `#161b22` | 同上 | ✅ |
| 结果窗表格交替行 | `#1a2029` | BG_ALT | ✅ |
| 日志面板文本区 | `#0d1117`（等宽+着色行） | — | ✅ |
| 选中导航强调色 | `#296ed1`（#2f81f7 hover 邻域） | ACCENT | ✅ |

**modlens 目检确认**（结构级）：TASK CONSOLE / BACKTEST CONSOLE 等 PageHeader 语义、RUN CONTROL 卡片、SYSTEM LOG 彩色日志行（INFO/WARNING/ERROR 分色）、结果窗口全量渲染正常。

## 四、§七 导出 PNG 深底化显式声明（审核补充①）

matplotlib 调色后，`export_report_images()` 导出的 **6 张报告 PNG（回测收益曲线/基本信息/交易记录/日收益/绩效分析-收益分布/绩效分析-月度收益热力图）将从深灰底（#2d2d2d 系）变为 GitHub Dark 底（#161b22 系）**——预期产物外观变更：
- `test_backtest_result_window_savefig.py` 全绿（仅颜色变化，无尺寸/生成异常）；
- 如有下游流程消费这些 PNG（报告归档/图像对比），需知会：色值系统性偏移，但曲线/语义色（红涨绿跌/橙基准）未变。

## 五、appendHtml 高频性能观察（审核补充②）

- 实现：`colorize_log_line` 为纯字符串拼接 + `html.escape`，单行微秒级；`setMaximumBlockCount(5000)` 保留。
- **实机观察项（请用户目验）**：采集/守护进程刷屏时段（如全量拉取分钟线）日志面板流畅度。
- **降级预案**：`quantstudio/gui/skin.py` 顶部 `COLOR_LOG_INFO = False` 一行开关——仅 WARNING/ERROR 着色，INFO 纯文本（无 span 开销）。

## 六、实机目验与功能冒烟（待用户执行）

### 6.1 实机端到端实测（已发生，2026-08-23 21:16-21:20）

用户实机启动 `python main_gui.py` 后**实际执行了一次完整回测**（非只目验）：

| 项 | 实测值 |
|---|---|
| 策略 | `weekly_smallcap_growth_momentum_10_quantstudio.py` |
| 区间 | 2026-01-05 ~ 2026-07-13（125 交易日） |
| 引擎审计 | 27 个调仓日 FUNNEL/REBALANCE/PORTFOLIO/FILL 审计全通过（selected=10，buy/sell 全成交，rejected=0） |
| 结果导出 | `output\backtest_results\20260823_212003_weekly_smallcap_growth_momentum_10_quantstudio` |
| 结果窗口 | 正常弹出，`export_report_images()` 生成 **6 张深色 PNG**（回测收益曲线 / 绩效分析-收益分布 / 绩效分析-月度收益热力图 / 基本信息 / 交易记录 / 日收益） |
| 日志面板 | 回测全程 337+ 条日志流经新 appendHtml 着色面板（21:18:53→21:20:04 持续刷屏 ~70s），无卡顿反馈 |
| 进程退出 | exit code 0（用户正常关窗） |

**结论：新皮肤下端到端真实使用（跑回测 → 结果窗口 → 深色导图 → 干净退出）全部正常，零异常。**

### 6.2 其余建议目验项（可选补充）

启动：`python main_gui.py`。建议清单：
1. 九个 Tab 逐一切换（PageHeader/深色卡片/表格边框）；
2. 采集任务：刷新/模式下拉联动提示/守护进程按钮五态（不实际启停亦可只看态）；
3. 数据浏览：执行 SQL、导出 CSV（原生弹窗应深色）；
4. 配置编辑：三个子页切换、保存采集任务（有确认弹窗）；
5. 质量检查：执行全部检查（表格填充）；
6. 策略回测：跑一个轻量策略→结果窗口弹出→6 张导图生成（深底）；
7. 导出 PTrade：打开转换对话框/日历选择器深色；
8. **日志刷屏时段流畅度**（补充②）。

## 七、回退方式

- 文件级：`git checkout -- <文件>`（皮肤未 commit，工作区即最新态）；两 M 文件另有 docs/handoff/ 文件级备份；
- 极端：`git reset --hard 32f04b32f3e4377991db53474d2312852b88c2a2`（会连同他人未提交改动一起回到基线，仅灾难恢复用）。

## 八、预存问题登记（非本皮肤范围）

P2/P3/P4 详见 `docs/handoff/gui-skin-baseline-20260823.md`（4+3 条测试夹具漂移、1 条模态挂起）——修复属测试/行为域，建议独立轨道，待用户裁决。
