# Q3 静默终止：取证**更正**与主嫌升级（2026-09-26 23:2x）

- 归属：客户运维会话 1｜性质：对 `docs/evidence/q3-silent-death-local-reproduction-20260926.md` 的**更正与推进**
- 触发：审核裁定「Q3 副本复现窗 GO」后，我先做了一步**零成本决定性判别**，结果**证伪候选①**

## 一、决定性判别（零成本）：同一个 2.29GB WAL 被成功回放 ⇒ **候选①证伪**

| 观测 | 值 |
|---|---|
| 新代际 PID 39296（22:42:09 起，`--mode forever`） | **存活、健康** |
| WAL | **2289.1 MB → 118.8 MB**（mtime 23:22:05）⇒ **回放 + 检查点成功收敛** |
| `daemon.log` 22:42 后 | **291 行**，23:20–23:22 正常批写 `etf_minutes`（`线1 还原 → normalize → aligner → validator → wrote 50000 rows`） |

⇒ **若回放会致命退出，则 39296 必死**；它没有 ⇒ **「WAL 尾未完成事务 → 回放致命退出」不成立**。
（原候选①的观察与排除项仍有效，但它们**不唯一指向**回放；本判别给出了反例。）

## 二、候选②（外部主体终止）**升为主嫌**——时序证据

| 时刻 | 事件 |
|---|---|
| 22:18:48 | 我停止旧 daemon(40060) |
| 22:20:56 | 我的 once 进程启动（仅 3 行日志：`单次执行模式 task=ALL` + `目标库校验`） |
| 22:20:56 → 22:39:5x | **19 分钟零日志、零 WAL 写入**（阻塞，无 banner） |
| **22:39:5x** | once 进程退出，**exit = -1**（TerminateProcess 语义） |
| **22:39:58/59** | **新 forever 实例(45036)** 被拉起（新 token `65834f35…`，`git_commit=a85cb9a`） |
| 22:40:01 | 我脚本的第二个 forever(11912) 被 `.daemon.lock` 拒绝并自退 |
| **22:42:10/11** | **又一次换代**：39296（token `a784f06d…`，`git_commit=0730fd8`）⇒ **2–3 分钟一轮的重新拉起** |

⇒ 存在**周期性拉起 forever 的外部主体**，且其在拉起前**终止了非 forever 的占位进程**（我的 once）——
exit `-1` 与「被杀时刻无输出」与此完全自洽；banner 未写、WAL 未动则说明被杀**发生在库打开/回放进行中**。

**嫌疑落点（机器事实）**：计划任务中存在
`\AgentQuant-Watchdog`、`\AgentQuant-Loop`、`\AgentQuant-Harness`
（命名直指守护/循环）——极可能即该外部主体；**下一步**：取其 `Task To Run` 与重复间隔
（`schtasks /query /tn <name> /fo LIST /v`），核对是否每条含「确保 daemon 运行」语义。

## 三、对 Q3 与 A′ 的影响（更新）

| 项 | 更新后的要求 |
|---|---|
| **Q3 定谳** | 由「(a) 回放致命」转向「**(b) 外部主体在回放期间终止 once**」；**客户侧**则需核对客户机**是否存在同类守护/监控**（若客户侧亦有"确保采集器运行"的守护，则其 12h 静默退出**很可能同源**：守护在长轮次中途终止进程并重启）⇒ **这是新的、可交付客户的排查方向** |
| **A′ 防护（强化）** | 除「CHECKPOINT 成功校验 + 失败即停」外，**必须确认窗口内无外部守护会拉起/终止进程**（否则 A′ 的 once 会被同样杀掉）；建议 A′ 前置：查计划任务态 + 必要时临时禁用相关守护（须用户裁） |
| 副本复现窗 | **降级为可选**：既然候选①被证伪，副本窗的针对性下降；**优先**做「守护主体确认 + 客户侧同类守护核对」 |

## 四、纪律留痕

- 本条**主动更正**已提交的候选定谳（`e9735f4`），依据是本轮零成本反例——按「禁止未证实归因」纪律执行；
- 未删改原档（保留推理与排除项），以**追加更正档**方式并存，便于审核对照两版证据。
