# P-D9 实施与验收证据 — filter_stock_by_status('ST') 转换语义一致化（2026-08-22）

- 关联方案：P-D9 方案 v3（审计通过，A/B/C/D/E 五条并入）
- 回退点：`git stash create -u -m "baseline-pd9-filter-status-<ts>"` → `2534692552ae7b2f8dc49eb75e7e6b9942676462`
- 关联登记：P-D9（转换语义不一致）/ 证据 `p-d9-pool-semantics-20260822.md`（根因）

## 1. 数据前提探针实证（probe_pd9_filter_ptrade.py，测试456 2026-08-22 05:15）

| 取证项 | 结果 | 决策影响 |
|---|---|---|
| ① circ_mv 平台可得性 | ❌ 不可得：`get_fundamentals('valuation', fields=['circ_mv','total_mv'])` → 平台报 `KeyError: ['circ_mv','total_mv'] not in index`（每日复现） | **market_cap 分支降级 price-only**（本地 DB 实测该分支零触发：source 分布 price 5913 / market_cap+both 0；降级差异面=零） |
| ② 批量取数性能 | ✅ 多码一次 0.007s / 8 码逐次 0.018s（每日稳定） | 幸存者兜底可行（~5,505 码批量 ≈ 数秒/日）；A 条三级设计落地 |
| ③ E 时点（08:30 vs 15:00） | ✅ **完全一致（EQUAL）**：before_trading_start 平台 get_history 已返回 T 日 close | **E 时点差不存在**（方案 v3 假设被实测推翻）→ 包装直接用当日 close，与本地 attach_day 同日同值 |
| ④ close 口径（0.9-1.1 临界带） | ✅ 600491/688496=0.92、仙股 0.15-0.35 等全部与本地 DB 一致 | D 条口径一致（仙股远离阈值） |
| ⑤ 缺省 filter_type | ✅ 隐式=显式 `["ST","HALT","DELISTING"]`（每日同集合） | B 条默认集合确认；同时实证平台原生行为：07-01 仙股 4 只全留池（P-D9 修复必要性直接证据） |

## 2. 实施内容

| 文件 | 改动 |
|---|---|
| `quantstudio/strategy_compiler/source_import.py` | `_QS_FILTER_STATUS_EXT` 注入模板 + 门控 `_source_uses_filter_status` + `_inject_all` 挂接 |
| `quantstudio/backtest/ptrade_api.py` | **仅注释行**（L820-824 证伪注释→实证结论，diff 逐行核对零行为） |
| `tests/test_ptrade_contract_compliance.py` | +10 P-D9 用例 |

模板要点：
- 包装：平台原生 filter → 'ST' ∈ filter_type（含缺省）→ 补 `close<1` 剔除；
- A 条三级：批量预取（`_qs_status_prefetch_closes` 多码一次 + 缓存命中短路）→ 幸存者兜底（仅对 result 判）→ 当日缓存复用；
- fail-open：取数失败 `log.warning` + 保持平台原生结果（与本地 `except: return result` 同语义）；
- 降级：circ_mv 分支平台不可得 → price-only（模板注释 + 登记表注明残余差异）。

## 3. 验收结果

| 项 | 结果 |
|---|---|
| 单元测试 | ✅ **117 passed**（107 基线 + 10 P-D9：门控 2/兜底逻辑 3/阈值边界 1/fail-open 1/缓存复用 1/本地同构 1/批量预取 1/缓存短路 1） |
| 精确命令与口径 | `python -m pytest tests/test_ptrade_contract_compliance.py tests/test_source_import.py -q` → 117 passed（test_ptrade_contract_compliance.py 77 + test_source_import.py 40） |
| 转换产物 | ✅ P-D9 包装/判别/批量/阈值/fail-open 五项全注入；白名单风险 ZERO；py_compile 通过 |
| 本地零漂移 | ✅ 本地回测净值 76,220.65 / -23.78% 与基线逐字段一致（模板只改转换产物） |
| ptrade_api diff | ✅ 仅注释行（git diff 逐行核对） |

## 4. 冒烟对账（已完成，2026-08-22 12:56/12:58 双端）

### 选股对比（P-D9 注入后）

| 席位 | 平台 | 本地 | 对齐 |
|---|---|---|---|
| 1 | 300255（800 股） | 300255（800 股 @25.09） | ✅ 代码+股数完全一致 |
| 2 | 300854（1000 股） | 300854（1000 股 @19.72） | ✅ 完全一致 |
| 3 | **600158（2900 股）** | **603272（800 股 @25.17）** | ⚠️ 唯一差异席位 |
| 4 | 600791（2200 股） | 600791（2200 股 @8.35） | ✅ 完全一致 |
| 5 | 603601（1600 股） | 603601（1600 股 @12.44） | ✅ 完全一致 |

**选股重合度：1/5 → 4/5** ✅（验收标准达成）

### 逐项归因

| 项 | 结论 |
|---|---|
| 仙股剔除 | ✅ 平台 4 只仙股（000004/002808/002898/300029）+ 临界带（600491/688496）全部不再出现——P-D9 兜底真平台生效 |
| 唯一差异席位（600158 vs 603272） | **D3 系横截面边缘差（收尾核对坐实，见下表）** |
| 订单结构 | ✅ 两端 5 笔单笔（正常股价未触发拆单） |
| 重合标的股数 | ✅ 300255=800 / 300854=1000 / 600791=2200 / 603601=1600 **逐只完全相同**（统一链前收 px 两端同值的最强实证——px 差 1 分钱股数即差） |
| fail-open 失败次数 | **0**（无 `P-D9 filter fallback failed` WARNING）——批量预取成功 |
| q 差异（0.9333/0.9000） | 已知 σ 窗口深度差，同判 reversal，不影响选股 |
| 退市强平 | 两端均无（仙股已不入池，D4-S3 连带消除） |

### 唯一差异席位边缘核对（ZCode 终审收尾项，本地 DB 复刻策略完整过滤后横截面 4,516 只）

| 排位 | 代码 | 12 月 ret（最跌榜升序） | 两端选中情况 |
|---|---|---|---|
| 1 | 002514 | -0.4828 | |
| 2 | 300854 | -0.4343 | ✅ 两端均选 |
| 3 | 300255 | -0.4322 | ✅ 两端均选 |
| 4 | 600791 | -0.4317 | ✅ 两端均选 |
| **5** | **603272** | **-0.4297** | 本地第 5 席 |
| 6 | 002667 | -0.4232 | |
| 7 | 603601 | -0.4198 | ✅ 两端均选（两端第 4/5 席收尾一致） |
| 8 | 300290 | -0.4041 | |
| **9** | **600158** | **-0.4025** | 平台第 5 席 |

- 第 4~7 名 ret 挤在 **-0.4198 ~ -0.4317（相对差 1.2% 窄带）**，第 5/6 名差仅 0.65%（相对 1.5%）；
- 两端横截面复权/快照微差（D3 系）足以令窄带内座次互换（本地第 5=603272 vs 平台第 9→第 5=600158）；
- 603601（第 7）两端均入选、且两端座次顺延一致 → 坐实**同带排序抖动**而非池差/框架差；
- **判定：D3-X2 类横截面边缘差，非 P-D9 缺陷** ✅

### 判定

**P-D9 修复达成设计目标**：平台候选池 = 本地候选池；选股从结构性分叉（仙股霸榜 1/5）收敛到边缘数据差（4/5，残留 1 席归 D3-X2 类）；验收标准第 3 条通过。

## 5. 影响面与回退

- 6 个含 'ST' 策略重转后候选池变化（vol_regime/ashare_manual_pool_2d/小市值策略2/小市值策略ptrade/first_cover_event_daily/smallcap_overnight_scalp_7）；本批仅重转 vol_regime，其余不重转（未重转产物逐字节不变）；
- 回退：撤销 `_QS_FILTER_STATUS_EXT` 注入块；回退点 `25346925` 在案。