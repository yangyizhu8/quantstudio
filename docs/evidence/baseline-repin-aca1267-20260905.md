# 基线重钉记录：aca1267 全绿态（2026-09-05，任务二备案）

- **依据**：总调度 2026-09-05 契约门独立实测 PASS（矩阵门哈希一致 68394f96 + 契约套件全过 + 白名单零触发），HEAD = `aca12674be8d9f2f3198f7114bf967e4d5a5f3a5`
- **性质**：基线重钉刷新（取代 duckdb-lock-regression-baseline-20260905.md 的"在途漂移层"阶段性口径——漂移已随三线并窗收官 aca1267 全部落地）

## 1. aca1267 后基线 = 全绿态（实测引用）

| 门 | 实测 |
|---|---|
| 矩阵哈希门 | ✅ wrapper_hash **68394f96**（含探针 v2 判据修正版）== yaml 记录，--check 一致 + MD 一致 |
| 契约套件 | ✅ 全过（p10 21 项含判型 probe 计账新契约 + fund_matrix 16 项含 §17 probe 计入预期） |
| 白名单 5 项 | ✅ 零触发（KNOWN_FAILS 允许失败未出现） |
| 数据源域 | ✅ 114+ passed（唯一化后语义锁定断言在卷） |
| 组装门禁 | ✅ 89 passed + 1 skipped（36+21 域 + 三新测试） |

## 2. 白名单 5 项现状（承接旧档 §1）

白名单允许失败本轮零触发；旧"在途漂移层 5 项"（fund_matrix 2 + compliance 3）已全部
**转正为新契约断言**（判型 probe 计入预期 / 现状锁定+报修触发器），不再依赖白名单豁免。

## 3. 基线锚

- HEAD：`aca1267`（三线并窗收官：A2 观测网 + B2 归一消音 + B2' 跨日窗口 + D4 缓存键 + 探针 v2 哈希追认）
- 前基线：`728948f`（§21 收口）→ `b33ba1f`（探针 v2 修正重建）→ `aca1267`
- 回退链：`295714a`（数据源唯一化）/ `b1ed785`（R5.5 fold 修复）/ `2287fa6`（§21 回滚）均 stash store 持久化在案

## 4. 在途知悉项（不改变全绿态判定）

- source_import.py 工作树 D4-S7 系列（他线进行中）——解除时走其六步；
- gap-range 登记失配（现状锁定断言在卷防恶化）——任务一合并方案包修复对象；
- chokepoint 双写入点（d960d33 8/3 预存在）——任务一合并方案包修复对象（#16 族）。