# 备用双禁用启用登记（预授权，一次性）

- 启用时间：2026-08-24 01:19（总调度 08-24 审计意见§7 裁定启用）
- 预授权依据：总调度 08-23 05:00 改排期指令第三条（用户已拍板）
- 禁用手段：非提权会话对 Trading\* 计划任务枚举被拒（实测：schtasks /Query 拒绝访问），
  改用**等效禁用** = 重命名 wrapper 脚本（任务计划动作指向文件不存在 → 空跑失败，可逆零提权）：

| # | 任务 | 窗口 | 禁用方式 | 恢复条件 | 恢复核对 |
|---|---|---|---|---|---|
| ① | Trading_Repair_Minutes | 周一 22:00-02:30 | `run_repair_minutes_scheduled.ps1` → `.ps1.disabled_20260824` | SNAP_003 create 完成后（周二 ~12:00 前，下一窗口周二 22:00 前） | 周二 22:00 任务正常执行 + 当晚日志新增 |
| ② | TradingCloudSync（增量） | 周二 03:00 | `run_cloud_sync.ps1` → `.ps1.disabled_20260824` | 同上（同步幂等，周三 03:00 自动补） | 周三 03:00 后 `logs/run_cloud_sync_*.log` 新增且水位推进 |

- 影响评估：repair 隔夜幂等续跑（wrapper 自述），增量同步漏一轮由周三 watermark 增量覆盖；
  全量同步（周日）不在禁用范围（run_cloud_sync_full.ps1 未动）。
- 双禁用与白名单修复**同时生效**（白名单作纵深防御，防未禁用任务族的 SYSTEM 子进程再次 abort）。
- 注意：两文件已含 v1.1 marker 代码，恢复原名即带 marker 上线。

## 恢复记录

- [x] 恢复①②：**由总调度执行**（08-24 03:12 "正确日期版"重写恢复；.disabled 文件已不存在）。
      稳定化会话 08-25 10:55 核验：两文件 qdb_domain_markers marker 代码在位、PARSE OK、
      markers 目录当前为空（无运行中 QDB 域任务，符合预期）。
- [ ] 周二 22:00 repair 执行核对：____
- [ ] 周三 03:00 同步执行核对：____
