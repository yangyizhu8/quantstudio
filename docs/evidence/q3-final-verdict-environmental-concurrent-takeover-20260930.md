# Q3 终版定谳（环境性）：并发会话 daemon 接管（2026-09-30）

- 归属：客户运维会话 1｜案件：jabberwock Q3「取数周期静默终止」+ 本机 A 受控窗异常退出
- 状态：**本机现象定谳闭环（环境性，非产品缺陷）**；**客户 Q3 回归其环境三件取证**（裁定①）
- 过程档：`docs/evidence/q3-silent-death-local-reproduction-20260926.md`（初版候选①）
  → `docs/evidence/q3-silent-death-reproduction-update-20260926.md`（更正：候选①证伪）
  → **本件（终版）**

## 一、四条假设的最终处置

| # | 假设 | 结论 | 依据 |
|---|---|---|---|
| ① | WAL 回放致命 → 原生终止（`exit 0xFFFFFFFF`） | **证伪** | 同一 2.29GB WAL，新代际 **39296 回放成功**：WAL **2289.1→118.8 MB**、22:42 后 291 行日志、正常批写 `etf_minutes` `wrote 50000 rows` |
| ② | AgentQuant-Watchdog 杀进程 | **证伪** | 审核亲读 `watchdog.ps1`(39 行) + `guard_main.ps1`(80 行)：仅管 agent-quant / trading-battle-back，**不触** QuantStudio daemon |
| ③ | 计划任务/外部守护拉起 | **无匹配** | 22:39–22:42 计划任务运行记录为空 |
| **④** | **并发会话 daemon 接管（成立）** | **定谳** | 另一 DSH 会话 `session-9cfa03f3`（QuantStudio 工作区，**正在做「GUI 委托通道 + 锁链」线**）会话日志含 `instance-token`×21 / `--mode forever`×122 / `--mode once`×78 / `terminate`×157 / 停 daemon×255，并含**「残留锁改名（持有者已确认死亡）」**与带新 token 的 forever 启动记录（如 `…--instance-token b1c4bb34…`、`ProcessId 45268`） |

**时序复原（本机）**：我的 once 22:20:56 启动（`单次执行模式 task=ALL`）→ 19 分钟零日志、零 WAL 写入
（阻塞于库打开/回放）→ **22:39:5x 被并发会话的接管流程终止（exit `-1`）** → 22:39:58 该会话拉起
forever `45036`（新 token `65834f35`）→ 22:42:10 再换代 `39296`（新 token `a784f06d`，存活至今）。

**为何「无 traceback + 无业务日志 + WAL 未动」**：进程在**库打开/回放阶段**（尚未进入业务、尚未写 WAL）
被**外部终止**（`TerminateProcess` 语义 ⇒ 无 Python 异常栈）。

## 二、裁定与影响

| 项 | 裁定/结论 |
|---|---|
| 本机复现性质（裁定①） | **环境性**（同工作区多会话共管 daemon）⇒ **非产品缺陷**，**不能据此定谳客户** |
| 客户 Q3 | **回归其环境三件取证**：① 客户版本号 `git log --oneline -1` ② 日志该时段前后 30 行原文 ③ 04:00–04:15 Windows 事件日志应用错误段（**客户环境证据不可替代**） |
| 客户侧新排查方向（保留） | 核查客户机是否存在同类「确保采集器运行」的守护/计划任务（分钟级轮询、检出即重启）——成本低、**直指静默退出形态**；可随下批通知交付 |
| 副本复现窗 | **撤销**（候选①已证伪；本机死因归因环境因素，无产品机理可复现） |
| A′ 修正窗（裁定③） | **走避让窗口**（与并发会话对表后择窗，不走副本库）；原三处脚本修正保留（CHECKPOINT 成功校验+失败即停 / 判据先于恢复采集 / 三验采样 ≥5min）；**第④条改为「事前声明占用」** |

## 三、纪律落位（裁定②已执行，无需另件）

跨会话 daemon 占用纪律**已由用户落位** `AGENTS.md` 铁律节：
**【铁律】daemon 生命周期跨会话占用纪律（2026-09-30 用户批准，Q3 并发接管案固化）**——
声明前置 / 避让不接管 / 专用窗口优先，**含本案实证**（客户运维 1 的 A 受控窗被并发会话接管终止、
窗口证据整段失效、白跑 19 分钟）。随下批推送生效。

## 四、副产品（正面收益，勿丢）

| 项 | 值 |
|---|---|
| **G4 判据达成** | `qfq_factor_revision_alert` 计数已 **> 0**（A 窗采集时 58 条）⇒ **T3 outbox 通道首次实弹成功** |
| 四项修复上产 | 新代际日志 `git_commit=a85cb9a`（= P2-1 提交）⇒ P1-2a/P1-2b/P1-4/P2-1 **已在生产实测** |
| 单实例闸有效 | 我误起的重复实例被 `.daemon.lock` 拒绝并自退（**无双 daemon 抢库**） |
