# PR6b-1 交接文档（2026-07-23，进行中）

> **新对话续接指南**：读本文件 + `.zcode/plans/plan-sess_36ee8868-8d20-4491-a9ef-9b719a10e7c0.md`（PR6a plan，PR6b-1 在其后续）+ `docs/strategy-compiler/pr6a-implementation-report.md`（PR6a 闭环基线）。PR6b-1 plan 已批准但未单独存为 plan 文件（在对话历史里），本文件是其唯一固化副本。

## 0. 一句话状态

**PR6b-1 进行中：CP1-CP5 已完成并自检通过，CP6-CP11 待做。工作区有未提交改动（CP1-5 的交付物）。最近 commit 是 `0004e1f`（数据运维）+ `398602b`（PR6a）。**

## 1. PR6b-1 plan（已批准，含 3 处修正）

PR6b 拆 PR6b-1（验证闭环）/ PR6b-2（golden cases）。PR6b-1 范围：
- 5 validators（validate_ptrade_portability / check_hard_filters / compare_strategy_variants / run_smoke_backtest / validate_strategy_spec 收敛）
- orchestrator.py（单点写 run_card，Orchestrator 模式，validators 保持纯函数）
- install_skill.py
- operation 扩展（pct_change/rank/top_n）+ **manual_list 最小多标的**（方案 A，解锁 rank 真实测试 + batch 差异层断言）
- 文档债务修复（minute DATA_MISSING stale → 实测 AVAILABLE）
- 测试 + pr6b1 报告

**3 处用户裁决修正**（相对 plan v1）：
1. §1.2 加 manual_list 最小多标的（3-5 只票），非 single_stock only。index_constituents 留 PR6b-2。
2. §4 case6 措辞：PR6b-1 交付 smoke 基础设施 + 分钟 smoke spec 实测；正式 golden case 6 进 PR6b-2。
3. 2 个实施注意点：① raise 消息 "PR6a"→"PR6b-1" 同步改测试断言（**已在 CP2 完成**）；② smoke subprocess 用 `encoding='utf-8', errors='replace'` + env `PYTHONIOENCODING=utf-8`（**CP6 待做**）。

**关键决策（已锁定）**：
- run_card 写入：Orchestrator 模式（validators 纯函数返回 tuple，orchestrator 单点写 run_card.json + variant_consistency_report.json）
- validate_strategy_spec 收敛 contracts.py（单一真源，5 timing 规则；防御性 import 失败给"请在项目根目录/venv 运行"）
- case 6 真实分钟 smoke（数据已在库：stock_minutes 18.78M / etf_minutes 46.94M）
- 成本透传进 PR6b-2（PR6b-1 只标 GAP）
- 黄金保护红线延续（etf_momentum/smallcap_guard/dual_ma_sample 不自动覆盖）

## 2. CP 进度（CP1-5 完成，CP6-11 待做）

### CP1 文档债务修复 ✅
- `docs/strategy-compiler/frequency-and-engine-profile.md` L24/27/29：minute-bar-v1 BLOCKED(DATA_MISSING) → READY；tick 保持 PLANNED
- `docs/strategy-compiler/implementation-status.md` L277/281/328/89/209/213：stale → 实测行数 + smoke PASS + end-labeled VERIFIED；历史注释标 superseded
- 自检：stale 扫描 CLEAN + tick PLANNED 保留 + minute READY + probe 一直正确

### CP2 operation 扩展 + manual_list 多标的 ✅
- `build_strategy_ir.py`：`_PR6B1_INDICATOR_OPS={ma,pct_change}`；`_PR6B1_RANKING_OPS={rank,top_n,bottom_n}`；pct_change 分支（numpy 内联）+ RankingNode 分支；dataload lookback 含 pct_change；raise 消息 PR6a→PR6b-1；manual_list codes 透传
- `render.py` `_build_template_context`：加 pct_change Indicator walk + RankingNode walk
- 4 templates：manual_list 多标的分支（QS emit get_history_batch / PTrade 循环 get_history）
- `contracts.py`：**RankingNode 前驱 bug 修复**（`_IR_PIPELINE_PREDECESSORS["RankingNode"]` 清空，由 `_EITHER_PREDECESSORS` 管；RankingNode either 扩展含 SignalNode）
- 自检：case1 不回归 33 passed + manual_list IR 承重（codes+lookback+max_positions）+ **遗留要求① batch 差异层真实触发**（QS emit get_history_batch / PTrade 禁）+ zscore raise 含 step id + PR6b-2

### CP3 validate_strategy_spec 收敛 contracts.py ✅
- `skills/.../scripts/validate_strategy_spec.py`：加 `_import_contracts_validate()`（防御性 import）+ `validate_spec` 调 contracts.validate_strategy_spec（5 timing 规则）
- 自检：case1 PASS + timing 违规被抓（`market_data_frequency must equal bar_frequency`，之前 Skill 脚本跳过的）

### CP4 validate_ptrade_portability + check_hard_filters ✅
- `validators/validate_ptrade_portability.py`：DENYLIST 6 API（get_fundamentals_batch/get_history_batch/create_dir/get_trades_file/get_research_path/convert_position_from_csv）。AST 扫 PTrade 产物，PORTABILITY-LOCAL-EXTENSION-BAN / PORTABILITY-FILE-DB-ACCESS
- `validators/check_hard_filters.py`：HARDFILTER-STOCK-13（全 13 项）+ EXECUTION-STAGE + EXECUTION-SUBSET（4-subset）+ ASSET-MATCH
- 自检：正向 PASS + batch/file 两类负向 BLOCK + 缺 execution-stage BLOCK

### CP5 compare_strategy_variants ✅
- `validators/compare_strategy_variants.py`：14 维度 QS vs PTrade。11 维度 Spec+IR IDENTICAL_BY_CONSTRUCTION；dim10 止损 EMPTY（PR6b-2）；dim13 成本 GAP（PR6b-2）；dim14 API AST 对比（qs_only batch 允许 / pt_forbidden BLOCK）
- 自检：manual_list 双产物 DIFF_OK + PTrade forbidden BLOCK + 成本/止损诚实标记

### CP6 run_smoke_backtest ⏳ 待做
- `validators/run_smoke_backtest.py`：读 capability_report.overall_execution_status；≠READY→smokeResult.status=BLOCKED + R6 line583 消息（不调引擎）；=READY→subprocess `python -m quantstudio.backtest.run_ptrade_strategy <qs.py> <start> <end> --profile <id>`。**注意点②**：subprocess encoding=utf-8 errors=replace + env PYTHONIOENCODING=utf-8。返回 tuple（含 smokeResult dict），orchestrator 写 run_card。
- smokeResult schema（run_card.schema.json L50）：{status: PASS|BLOCKED|FAILED, command: str, summary: str}

### CP7 orchestrator.py ⏳ 待做
- `quantstudio/strategy_compiler/orchestrator.py`：端到端入口 spec→build_strategy_ir→render→7 validators→收集 tuple→写 run_card.json + variant_consistency_report.json。状态转换单点（SPEC_ONLY→STATIC_VALIDATED→SMOKE_EXECUTED）。黄金保护。CLI `python -m quantstudio.strategy_compiler.orchestrator <spec> [--start] [--end]`
- 无现有 run_card writer，PR6b-1 创建。validator→run_card 字段映射：validate_strategy_spec→validation.schema；scan_lookahead→timing；check_hard_filters→hard_filters；validate_ptrade_portability→api_portability；compare_strategy_variants→variant_consistency + 写 variant_consistency_report.json；run_smoke_backtest→smoke_backtest+stage+status

### CP8 install_skill.py ⏳ 待做
- `skills/.../scripts/install_skill.py`：默认 source=skills/quantstudio-strategy-compiler，dest=~/.agents/skills/（运行中 agent 读的根）。_validate_skill→copytree→subprocess quick_validate→失败 rmtree rollback。CLI `[--source][--dest][--name][--force][--skip-validate]`

### CP9 测试 + pr6b1 报告 ⏳ 待做
- `tests/test_pr6b1_validators.py`：5 validators 正向+负向。**遗留要求②**：failure case（capability BLOCKED / portability BLOCKED / tick PLANNED）断言"被阻断"非"通过"
- `tests/test_pr6b1_orchestrator.py`：case1 端到端 run_card SMOKE_EXECUTED + tick spec BLOCKED 路径
- `tests/test_pr6b1_operation_expansion.py`：pct_change/rank/top_n + manual_list + **遗留要求①** batch 差异层断言 + zscore raise PR6b-2
- `tests/test_pr6b1_install_skill.py`：install tmp_path + quick_validate + rollback
- `docs/strategy-compiler/pr6b1-implementation-report.md`：文档债务纪律 + operation 边界 + orchestrator 架构 + 成本/止损 GAP

### CP10 验收 ⏳ 待做
- pytest test_pr6b1* 全绿 + 全量 pytest -q ≥422 + quick_validate PowerShell（Remove-Item Env:PYTHONUTF8）+ orchestrator 跑 case1（真实 smoke，数据已在库）

### CP11 commit ⏳ 待做
- 严格暂存（PR6b-1 文件不混 PR6b-2 预备）。message 含验收证据。

## 3. 续接时第一步必做（验证未回归）

新对话开局先跑：
```
cd D:/miniQMT策略实盘/QuantStudio
python -m pytest tests/test_pr6a_case1_e2e.py tests/test_pr6a_validators_negative.py -q
# 预期 33 passed（CP1-5 改动不应破坏 PR6a）
```
+ 确认 `git status` 显示 CP1-5 的未提交改动（docs + build_strategy_ir + render + contracts + 4 templates + 3 new validators + validate_strategy_spec）。

## 4. 已知风险/注意点

- **smoke 真跑需 DB 可用**：CP6/CP10 的 READY 路径实测会调 backtest 引擎，daemon 持锁时等待（同 PR6a fidelity gate）。先用 mock capability 测 BLOCKED 路径，READY 实测留 CP10。
- **tick spec BLOCKED 路径**：inspect_capabilities.py 对 tick 返回 PLANNED（不变量 4），run_smoke_backtest 应输出 BLOCKED 不调引擎。这是遗留要求②的 case 10。
- **全量 pytest 的 daemon 锁**：test_batch_apis/test_strategy_alignment 在 daemon 持锁时失败（环境问题非回归），daemon 空闲后重跑。
- **manual_list 模板的 handle_data 用 scores dict**：当前 pct_change 写 scores，但 RankingNode 的 sorted 用 scores——模板变量名要对齐（CP2 已验证 compile PASS，但 CP9 测试要确认渲染产物语义正确）。

## 5. 关键 file:line 参考

- operation 常量：`build_strategy_ir.py:46-48`
- signal chain dispatch：`build_strategy_ir.py:237-335`
- RankingNode 前驱修复：`contracts.py:136-148`（_IR_PIPELINE_PREDECESSORS）+ `contracts.py:197-201`（_EITHER_PREDECESSORS）
- render context walk：`render.py:146-169`
- manual_list QS 模板：`skills/.../templates/quantstudio_daily.py.j2`（{% elif universe_codes %}）
- manual_list PTrade 模板：`skills/.../templates/ptrade_daily.py.j2`（{% elif universe_codes %}，循环 get_history）
- PTrade DENYLIST：`validators/validate_ptrade_portability.py:30-37`
- 13 filter 集：`validators/check_hard_filters.py:24-27`
- smokeResult schema：`schemas/run_card.schema.json:50`
- backtest CLI：`quantstudio/backtest/run_ptrade_strategy.py:115-198`
- capability overall_execution_status：`schemas/capability_report.schema.json:13`

## 6. 不要做的事

- 不要重做 CP1-5（已完成自检）
- 不要把 manual_list/index_constituents 混淆（manual_list 在 PR6b-1，index_constituents 在 PR6b-2）
- 不要在 PR6b-1 commit 混入 PR6b-2 预备（Factor ops/ETF/成本透传/9 golden cases）
- 不要放宽 smallcap_guard 容差（已在 0004e1f re-baseline，locked）
- 不要改 tick PLANNED 状态（不变量 4，PR9 scope）
