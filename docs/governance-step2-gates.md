# 治理方案实施第 2 步交付物 — D2 硬门槛检查（裁定版 v2）

- 状态：**D2 部分完成（FAIL，差异全部归因分类并登记）；D3 巡检与分钟二选一待做**
- 日期：2026-08-17
- 依据：`docs/project-stabilization-plan.md` §2.1.3 + DSH 审计裁定（G1 公共窗口 / G2 容差证据 / 002029 不豁免）
- 脚本：`scripts/governance_d2_gate.py`（只读；确定性 hash 抽样 `order by hash(code, time)`，可复现）
- 报告：`output/golden_baseline/d2_gate_report.json`（v2）

---

## 1. G1 公共窗口行数对账（裁定 1 执行）

**公共窗口定义（脚本化推导，无手工指定）**：`[max(两端 min), min(两端 max)]`，逐表由两端实测窗口计算（报告 `tables.*.common_window`）。

**设计性窗口出处（裁定条件②）**：
- `config/collector_tasks.json`：19 个采集任务中 16 个 `start_date="2018-01-01"`；
- `quantstudio/pipeline/daemon.py:571`：默认 `start_date="2018-01-01"`。

**结果：公共窗口内 FAIL（8 表）**：

| 表 | 公共窗口内差（QDB−Duck） | 初步定性 |
|---|---|---|
| stock_daily | **4130** | 真实同步缺口（量小，200 抽样未命中行级错配；需按日期段×代码定位） |
| etf_daily | 391 | 真实同步缺口（同上） |
| index_daily | 43 | 真实同步缺口 |
| etf_basic | 29 | 真实缺口（静态表全量对账口径） |
| etf_dividend | 13 | 真实缺口（与已登记"744 条公司行为特征"相关） |
| stock_basic | 5 | 真实缺口 |
| stock_minutes | 2,567,982 | **覆盖稀疏**（见 §4 专项） |
| etf_minutes | 36,568,521 | **覆盖稀疏**（见 §4 专项） |

**窗口外差异**：已按裁定条件③登记为已知噪声（设计性窗口，QDB 2000-2017 部分本地不同步，出处见上），报告 `known_noise` 字段自动生成。

**末端增量缺口**（裁定条件④）：duck stock_daily max=2026-08-13 < qdb max=2026-08-14，**登记为 D1 级待办工单**（恢复增量同步），非接受现状。

## 2. G2 抽样对账（裁定 2 执行）

- 抽样：确定性 hash 排序，200/表。
- **容差证据（裁定条件①）**：抽样内全部容差内对账行的实测最大舍入残差 = stock_daily **4.988e-4**、etf_daily **4.799e-4**，均 ≤5e-4 → 5e-4 容差由实测证据支撑，生效。vol 全部一致（×100 换算 + 1e-3 epsilon）。
- **etf_daily：PASS**（200/200 checked，0 miss，0 real_diff）。
- **stock_daily：FAIL —— 7 笔 real_diff（逐笔列明，全部 vol 一致、全部为 qfq/front 复权因子差异）**：

| # | 代码@日期 | QDB qfq | 本地 front | 差值 | 备注 |
|---|---|---|---|---|---|
| 1 | 920445.BJ @2022-11-11 | 6.429 | 10.790 | **4.361** | 北交所，因子差最大 |
| 2 | 600060.SH @2023-09-08 | 18.589 | 19.275 | 0.686 | |
| 3 | 000921.SZ @2021-05-25 | 12.268 | 12.860 | 0.592 | |
| 4 | 301297.SZ @2023-12-22 | 25.755 | 25.808 | 0.053 | |
| 5 | 688098.SH @2020-02-24 | 21.154 | 21.173 | 0.019 | |
| 6 | 600675.SH @2019-09-02 | 4.146 | 4.164 | 0.018 | |
| 7 | 688599.SH @2024-10-10 | 22.19 | 22.20 | 0.010 | 裁定追问项：本次确定性抽样中复现为 real_diff（dc=0.010 > 5e-4），非舍入类、非消失 |

**北交所核查结论（裁定条件③-②）**：v1 的 2 笔 miss 在 v2 修复后缀映射（92/83/43/87/88 → .BJ）后消失（200 抽样 0 miss），证实为**脚本映射缺陷，非本地数据缺失**（本地 340 个北交所前缀代码与 DSH 实测一致）。920445.BJ 作为 real_diff 出现，属真实因子差异。

**002029（裁定指定项）**：本次确定性抽样未抽中；已单列登记（见 §5 D2-F1 组），与上表 7 笔合并为"历史复权因子差异"归因工单——**第 4 步基线建立前必须归因完成**（防呆①：复权因子直接影响回测价格序列）。

## 3. G3：blocked = 0 ✅

## 4. 分钟表覆盖矩阵专项（裁定条件⑤）

| 表 | 本地月范围 | 本地覆盖交易日 | 本地 distinct codes | 公共窗口内 QDB 覆盖 |
|---|---|---|---|---|
| stock_minutes | 2026-06 → 2026-08 | **39 天** | 5227 | 全窗口（差 256 万行） |
| etf_minutes | 2025-01 → 2026-08 | **285 天** | 1658 | 全窗口（差 3657 万行） |

**结论**：本地分钟覆盖稀疏是**结构性现状**（与 profiling 报告吻合：stock_minutes 仅 2026-06-15 起、39 个交易日）。**登记为高优先级已知噪声**，直接影响分钟基线候选可行性——按 DSH 预判，实测二选一大概率走向"降级为登记项，三策略基线退化为两策略先行（S2 覆盖以分钟数据补齐为登记条件）"，以第 2 步剩余实测为准。

## 5. 新增登记项（并入登记表首批存量）

| ID | 级别 | 内容 |
|---|---|---|
| D1-TODO-1 | D1 | 末端增量缺失（duck max 08-13 < qdb 08-14）→ 恢复增量同步工单 |
| D2-F1 | D2 | 历史复权因子差异 8 笔（002029 + 上表 7 笔），基线建立前归因到具体分红送转事件 |
| D2-F2 | D2 | 公共窗口内行数缺口：stock_daily 4130 / etf_daily 391 / index_daily 43（按日期段×代码定位；处置二分法：可解释数据属性 → 登记"已解释差异"，疑似同步缺陷 → 修复工单走唯一写入会话） |
| D2-F3 | D2 | 静态表缺口：etf_basic 29 / etf_dividend 13 / stock_basic 5（同 F2 处置二分法） |
| D2-F4 | D2 | **index_daily 窗口异常（DSH 审核新发现，已复核证实）**：本地窗口 [2025-04-07, 2026-08-03]，起点晚于设计窗口（2018-01-01，QDB 自 2010-01-04 全覆盖）且末端落后其他表 10 天（08-04 后 0 行）；43 行公共窗口差优先按末端缺失归因；核查其独立同步任务（collector_tasks.json:195 tushare 源）；三策略 set_benchmark('000300') 依赖此表，2026-07 基线区间内基准存在可跑 |
| D3-KN-1 | D3 | 分钟覆盖稀疏（stock 39 天 / etf 285 天）高优先级已知噪声，分钟基线降级判定输入 |
| KN-1 | 已知噪声 | 设计性窗口外差异（2000-2017，出处 collector_tasks.json） |

## 5.5 D3 门槛判定行（2026-08-17 补，DSH 审核要求）

**未登记异常 = 0，D3 关闭。** 三类产出 100% 归入已登记/已解释类别：

| 类别 | 数量 | 归类 |
|---|---|---|
| 停牌 1220 只·日 | 1215 笔 @07-01 + 1 @07-07 + 3 @07-29 + 1 @07-30 | 07-01 群 → D3-KN-2（模拟数据特征待云端定性）；159739@07-29 等零星 → 对齐取证已解释（真实停牌，QS_FILL_AUDIT 案例正源于此） |
| 公司行为 758 条 | 绝大多数 @07-02 群 | D3-KN-2（同上，与 S1-2 登记项合并语义） |
| 边界样本 151 条 | **149 笔 @07-02 + 1 @07-03 + 1 @07-29** | 149 笔与 758 条同属 07-02 CA 群（阈值 ±1.5% 邻域，同一现象）；2 笔零星属已解释停牌日 |

即：无任何一笔落在"未登记、未解释"类别。D3 门槛 PASS（以登记表已有条目为口径）。

## 6. 待办（第 2 步剩余）

- [x] D3 巡检：`audit_etf_corporate_actions.py` + 已知噪声登记清单（合并 KN-1/D3-KN-1 与 17 项会话存量，登记表首批落盘）
- [ ] **D2-F2/F3/F4 归因任务**（DSH 审核补入）：公共窗口内行数缺口（stock_daily 4130 / etf_daily 391 / index_daily 43 / 静态表 29/13/5）按日期段×代码逐类定位，结果分"已解释差异（登记）/ 同步缺陷（修复工单，走唯一写入会话）"两类处置
- [ ] D2-F1 复权因子 8 笔归因（第 4 步前完成）
- [x] 分钟候选实测二选一：**选定 smallcap_overnight_scalp_7_quantstudio.py，不降级**（见 §6.5 定案）

## 6.5 分钟候选二选一定案（2026-08-17，实测裁决）

**结论：选定 `smallcap_overnight_scalp_7_quantstudio.py` 为分钟 Profile 基线策略，三策略基线维持，不降级。**

| 判据 | 结果 |
|---|---|
| 完整跑通 | ✅ 2026-07-01~07-31 全月 23 交易日，无异常中断 |
| 无异常中断 | ✅ 选股→低开买入→10:30 退出→收盘对账全流程正常（30 笔成交） |
| 产物可复现 | ✅ **两跑全部 7 个产物逐字节一致**（trades / daily_stats / benchmark / config / round_trips / ptrade_metrics.csv+json，cmp 全 IDENTICAL） |

**方法学加强**：两跑均在**未显式设置 PYTHONHASHSEED**（进程随机哈希种子不同）条件下取得逐字节一致 → 策略对迭代顺序无依赖，结论强于"同种子一致"的审计最低要求。正式基线运行规范仍固定 `PYTHONHASHSEED=0`（DSH 审核补充要求），作为防御性规范。

**该基线的已知噪声属性（登记，防自证复现误判；DSH 审核要求三项齐）**：
1. 07-17 分钟缺口（在基线区间内，引擎以 bar 驱动容忍）；
2. 07-01 ETF 停牌群（1215 只，D3-KN-2）；
3. 07-02 ETF 公司行为群（758 条，D3-KN-2）。

此三项若在复现时产生日级微变，先对照本清单归因，不判新问题。

**成本记录**：单次运行 1662~2459 秒（热/冷缓存差异），S6 预算内但两级预筛必须落地。

## 6.6 qfq_invariant 参数化实施验收（2026-08-17，微流水线完成）

改动说明 `docs/governance-qfq-invariant-param-spec.md` 经 DSH 审计通过后实施：
- 改动：`check_golden_rows()` 新增末位参数 `main_db_path=None`，自开连接分支改用 `db_path()`（≡ 旧硬编码，逐字符等价）；
- 验收①默认等价：真实主库默认调用 `checked=1, mismatched=0, skipped=0`（与改动前一致）；
- 验收②副本指向：`main_db_path` 指向含哨兵行的副本 → 哨兵命中 `mismatched=1`（读到指定库）；
- 验收③测试：qfq_invariant 相关 15 个既有用例全过（默认调用零行为变化）；
- 回退：单点改动 revert 即回退。随方案第 8 步统一确认推送，不单独推送。

## 7. 流程语义（DSH 审核确认）

D2 当前 FAIL ⇒ 第 4 步基线建立被阻塞（预期工作流非异常）。处置路径：D2-F1~F4 + D1-TODO-1 全部处置 → 重跑 D2 复检 PASS → 建基线。**第 3 步快照机制不依赖 D2 PASS，可并行启动**（快照只固化现状，干净性由门槛判定）；启动前先落实记录项 B（qfq_invariant 参数化）。

## 8. B 线 D2-F1 复权因子 8 笔归因结论（2026-08-17，只读分析）

方法：qfq_aux 本地因子链（裸码）与 QDB adj_factor 链按日对齐，归一化比值找首个分叉日，再核查分叉日两端因子跳变。

| 案例 | 首分叉日 | 归因 | 判定 |
|---|---|---|---|
| 002029 @2019-06-21 | 2019-06-26 | **除权生效日差 1 天**：两端因子值完全一致（9.554→9.702），本地 06-26 跳变、QDB 06-27 跳变，仅过渡单日 front 值不同 | 已解释（口径差） |
| 688098 @2020-02-24 | 2021-01-06 | 同上（1.0037→1.004 两端一致，本地早 1 天） | 已解释（口径差） |
| 600675 @2019-09-02 | 2020-07-22 | 同上（40.40 vs 39.14 为同事件不同生效日） | 已解释（口径差） |
| 000921 @2021-05-25 | 2021-07-08 | 同上 | 已解释（口径差） |
| 600060 @2023-09-08 | 2024-07-30 | 同上 | 已解释（口径差） |
| 301297 @2023-12-22 | 2024-06-24 | 同上（微小分红 0.04%，两端单日翻转时序不同） | 已解释（口径差） |
| 920445.BJ @2022-11-11 | 2023-05-22 | **QDB 侧自不自洽**：QDB qfq close=6.429 = raw×0.536，而其自身 adj_d/adj_latest=0.899（raw×0.899=10.79=本地 front ✓ 本地自洽）——北交所代码云端侧 qfq 值与自身因子链矛盾 | 已解释（云端侧 BJ 数据缺陷） |
| 688599 @2024-10-10 | — | 两端因子比均为 1.0，QDB qfq close=22.19 vs 其自身 raw=22.2（0.045% 不一致）——云端侧个别值缺陷 | 已解释（云端侧） |

**结论**：
1. **8 笔全部归因完毕，零笔指向本地因子链错误**——本地 front 全部与自身因子链自洽；不生成修复工单，**D2-F1 关闭（已解释）**；
2. 6 笔为除权生效日口径差 1 天（本地按除权日生效，符合 A 股惯例；QDB 晚一天）——绝对除权日以交易所公告为准，列入外部核对清单（不阻塞基线：仅影响除权日单日 front，且基线绑定本地快照自洽）；
3. 2 笔为云端侧数据缺陷（920445.BJ 因子不自洽、688599 个别值偏差）——登记 D1 层待云端定性；
4. 防呆①满足：本地价格链无病灶，基线不会固化带病价格。

## 9. B 线 D2-F2/F3/F4 归因结论（2026-08-17，只读分析）

### 9.0 前置勘误：v2 报告 G1 公共窗口数字存在脚本时区 bug

按日聚合复算发现：v2 脚本 duck_cw 的 epoch 过滤把"每日 0 点 CST 时间戳"错切（首日整日被排除）。**真实公共窗口差：stock_daily 849（非 4130）/ etf_daily 20（非 391）/ index_daily 13（非 43）**。QDB 侧数字无误（9,627,056），仅 duck 侧窗口计数偏小 3,281。D2 复检脚本须改用 strftime 分组口径后重跑出正式数字。

### 9.1 F2 行数缺口归因（按日×代码定位）

| 表 | 真实差 | 归因 | 处置 |
|---|---|---|---|
| stock_daily | 849（835 个差异日，每日 1-3 行） | ① `600069` **整码缺失**（本地 0 行，QDB 全历史约 400+ 行，长期停牌股）；② 北交所 920xxx 零星散缺（本地 BJ 已有 31 万行，个别日缺） | 同步缺陷 → 修复工单（600069 全量 + BJ 补缺，走唯一写入会话） |
| etf_daily | 20（8 天） | 07-01 缺 1（=S1-1 已登记回填缺口）；07-29 缺 3；08-04~08-12 缺 ~16（近期增量滞后） | 同步缺陷 → 并入 D1-TODO-1 增量恢复工单 |
| index_daily | 13（2 天） | 2026-08-03 当日 31 缺 12（末日部分同步）；2025-04-07 首日缺 1 | 同步缺陷 → 并入 D2-F4 工单（index_daily 任务核查+补拉） |

### 9.2 F3 静态表缺口归因（行级核对）

| 表 | 差 | 归因 | 处置 |
|---|---|---|---|
| etf_basic | 29 | 29 只**新上市 ETF**（158xxx/159xxx 近期上市）本地未同步 | 同步缺陷 → 增量补拉工单 |
| etf_dividend | 13 | 日期格式差导致的假差异已抵消（159118/159119 等 epoch↔ISO 同行配对）；真实缺 = **近期月度分红事件**（159119@08-17、159209@2025 系列）未同步 | 同步缺陷 → 增量补拉工单 |
| stock_basic | 5 | 5 只**近期 IPO**（001232/301707/301717/603468/688828）未同步 | 同步缺陷 → 增量补拉工单 |

### 9.3 F4 index_daily 窗口异常归因

起点 2025-04-07 晚于设计窗口：index_daily 任务（collector_tasks.json:195，tushare 源）历史区间未回填至 2018——**结构性覆盖缺口**（非最近损坏）；末端 08-03 后缺失并入 D1-TODO-1。基准 000300 在 2026-07 基线区间内存在（可跑）。处置：登记为已知覆盖现状 + 补拉工单（低优先，基准类数据按需补）。

### 9.4 汇总处置

F2/F3/F4 全部归因完毕：**零笔"已解释数据属性"，全部为同步缺陷 → 合并为一个"增量同步恢复+补拉"修复工单包**（含 600069/BJ 补缺、ETF/IPO 增量、index_daily 补拉、末端增量），修复执行等 A 线快照机制落地后走唯一写入会话。修复完成后重跑 D2 复检（须先修 G1 脚本时区 bug）。

## 10. D2-F1 补证（DSH 第 4 轮审计重开后，2026-08-17）

证据文件：`output/golden_baseline/d2_f1_evidence.json`（sha256 前 12 位 af6ced034538，含六项要求中①③⑥原始数据）。

### 10.1 六项要求落实情况

| # | 要求 | 落实 |
|---|---|---|
| ① 样本日两端 raw/adj_i/adj_latest/computed_front | evidence.json `sample_day` 字段（8 案齐） | ✅ |
| ② anchor 锚定日期/版本/来源 | 两端 anchor 均为 2026-08-13/14（QDB 多一日=08-14 增量滞后），来源：本地 qfq_aux adj_factor 链 / QDB stock_daily adj_factor 列 | ✅ |
| ③ 分叉日前后因子序列 | evidence.json `divergence_window`（±6 天两端逐日因子） | ✅ |
| ④ anchor/as-of 机制 | **证实**：两端 qfq close 均为物化快照值——QDB 的 is_qfq close 按同步时点锚计算后不再重算；本地 close_front 按重锚管线维护。差异本质是"锚的 as-of 版本不同" | ✅ |
| ⑤ 交易所除权公告 | **无法本地取证**（stock_dividend cash 列 NULL）→ 列外部核对 | ⚠️ 外部 |
| ⑥ 920445/688599 原始查询+hash | evidence.json `raw_query_6` | ✅ |

### 10.2 修正后的归因结论（推翻 §8 部分结论）

本地链自洽性逐案验证（front =? raw × 本地adj_i / 本地anchor）：

| 组 | 案例 | 结论 |
|---|---|---|
| **A 组：本地自洽（6/8）** | 920445 / 000921 / 301297 / 688098 / 600675 / 688599 | 本地 front 与本地因子链精确一致；G2 差异 = **QDB 侧 stale as-of 锚**（同步时点锚与当前锚之差）。云端侧，无本地修复 |
| **B 组：本地重锚缺口（2/8）** | **002029 / 600060** | **本地缺陷**：最近链事件（2026-08-10 / 08-06）重锚**未覆盖历史行**——事件前的行全部 stale（如 002029 历史行 front 差 1.011 倍=恰为 08-10 事件比例），事件后的行自洽 |

### 10.3 B 组影响范围（量化）

- 2026-08 有因子事件的代码共 **52 个**；
- 抽样 25 个中 **19 个（76%）** 存在历史行 stale（含 300750——与登记表 S2-2"canary 300750 恒定 0.364% 偏差"吻合，根因找到）；
- 估计 **~40/52 码**历史 front 需重锚刷新。

### 10.4 处置（修正）

1. D2-F1 **维持打开**：A 组 6 笔登记"已解释（云端 stale as-of）"；B 组升级为**本地重锚修复工单**（~40 码历史行 front 刷新，走唯一写入会话，等 A 线快照落地后执行：写前快照 → 修复 → 写后快照 → D2 复检）；
2. S2-2（canary 300750）根因确认=B 组，修复后自然消化；
3. §8 的"8 笔零本地错误"结论**撤销**，以本节为准；
4. 第 4 步基线继续阻塞（B 组修复 + D2 复检 PASS 前不得建立）。

## 11. D2-F5 strategy_events 重复导入处置方案（2026-08-17，快照键唯一性实测发现，待执行）

**现状证据**：2657 行；(event_type,event_date,code) 3 组重复（002792.SZ / 603861.SS / 002380.SZ，均 first_cover@2026-06-26，差异在 imported_at/source_row_id）；其中 1 组**全列完全重复**（去重后 2656）。

**处置方案（DSH 三审裁定框架内）**：
1. 全列完全重复 1 行 → **删除**（无信息损失）；
2. 3 组非完全重复 → 视为重复导入的同事件版本，**按业务键去重保留最新 imported_at**（first_cover 事件语义为一次性事件，非版本化数据）——如业务确认需保留版本，改登记为已解释并保留，处置前须业务确认；
3. 处置时证据要求：处置前后行数（2657→预期 2653）+ 处置前后表级逻辑 hash（按 sort_keys.json 全列排序口径）；
4. 执行通道：纳入"增量同步恢复+补拉+清理"唯一写入工单（等 A 线快照落地，写前快照→处置→写后快照→D2 复检）；
5. 快照侧：全列 canonical 排序仅保证 hash 确定性，不掩盖重复（DSH 确认），D2-F5 保持登记至处置完成。

第 4 步基线建立前置新增：D2-F5 处置完成（或业务确认保留为合法版本并登记已解释）。
