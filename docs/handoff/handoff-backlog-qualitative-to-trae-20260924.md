# 移交件：挂账定性批 → Trae 主修单后续批次（经用户转发）

- **移交方**：客户运维会话 2（dsh 侧）
- **接收方**：Trae（QFQ 域主修单归属方）
- **日期**：2026-09-24
- **性质**：**成果移交 + 工作项转派**（非问题上报）
- **用户裁定（2026-09-24）**：
  - ② **补 `class_av_raw` 工件批准** → 请分类引擎出一版含**裁定前原始分类**的 detail
    （253 行级逐行定性才可做）→ 转 Trae 排
  - ③ **V-1 剩余 11 码（`k_int=0` 组）+ V-3 第二层 + V-2 逐行定性** → **并入 Trae 主修单后续批次**
    （**不立新追单**——qfq 域统一管理防碎片化，主修单收口时一并排程）
  - ④ **V-4 NO_ADJ** → 转**数据域新发现登记**（因子通道覆盖问题独立于主修，列入下次云端交互议题
    或 Trae 后续批）

---

## 一、本会话已完成（可直接作为输入，无需重复勘察）

| 件 | 结论要点 |
|---|---|
| `docs/evidence/backlog-v1-x-class-verdict-20260924.md` | **X 类定谳**：整数次施加 + **全局锚偏移 ≈0.87%**（五重证据：幂律拟合 / 双通道同判 / 抖动方向反证 / `c/k_int` 比例指纹 / 校正回收敛） |
| `docs/evidence/backlog-v2-r-class-verdict-20260924.md` | **R 类**：2,184 计数逐位一致；12 码中 **7 只 `r_max≈2/3`**；甲类 7 码贡献 98.1% |
| `docs/evidence/backlog-v3-d-class-verdict-20260924.md` | **D 类**：512250.SH 单码**恒定 ≈1.97% 乘子失真**（f 离散 ±0.03%、幂律 R²=0.012）⇒ 整码挂起正确 |
| `docs/evidence/backlog-v4-noadj-verdict-20260924.md` | **NO_ADJ**：167 码 / 4,441 code-day；三张因子表 **0/167**；**`etf_basic` 亦 0/167** |
| `docs/handoff/errata-plan-v1-rmax-20260924.md` | §九 V-1 `r_max` 区间勘误块（**可直接粘贴**）；另附 V-2/V-4 两处建议勘误 |

**取证脚本（全只读，可复现）**：`QuantStudio/scripts/acceptance/probe_x_class_decisive.py` ·
`probe_x_class_implied_power.py` · `probe_x_class_final.py` · `probe_x_class_third_source.py` ·
`probe_r_noadj_direction.py` · `probe_backlog_qualitative_1.py` / `_2.py` / `_234.py` ·
`probe_triage_artifacts.py` · `probe_backlog_source.py`

---

## 二、转派工作项（三项 + 一项登记）

### W-1：补 `class_av_raw` 工件（用户裁定②）

| 项 | 内容 |
|---|---|
| 目标 | 分类引擎输出一版 detail，**含裁定前原始分类**（`class_av_raw`） |
| 原因 | 现 `detail.csv` 保存的是**已按「日线锚优先」裁定后**的 `class_av` ⇒ **B→A 2,162 例的逐行明细不可得**（实测仅 1 行） |
| 价值 | 使 **253 行级**（B→A 2,162 等的逐行）定性可做；亦可用于复核 X/D 的裁定链 |
| 验收 | 新工件中 `(class_av_raw, class_literal)` 的交叉表计数 **应复现 `agree_matrix`**（尤其 B→A=2,162、A→C=22） |
| 归属 | Trae（分类引擎） |

### W-2：V-1 剩余 11 码第二层定性（`k_int = 0` 组）

| 项 | 内容 |
|---|---|
| 对象 | 11 只 X 码，拟合 `k_int=0` 且 **R²≈0**（无幂律关系） |
| 线索（本会话实测） | 511010.SH（n=177, c=−0.0502, j_av 中位 **1.715**）· 159547.SZ（n=169, c=−0.0396, j_av **1.334**）· 159220.SZ（n=37, c=−0.0196, j_av 0.749）· 159221.SZ（n=61, c=+0.0063, j_av **−0.307**）· 513920.SH（n=66, j_av −0.289）· 560610.SH（n=153, j_av −0.272）· 510180.SH（n=59, j_av −0.350）· 159355.SZ（n=93, j_av −0.300）· 159232.SZ（n=177, j_av −0.323）· 520660.SH（n=164, j_av −0.354）· 512250.SH（n=76, j_av −0.453） |
| 观察 | 该组 `f` 与 `r` 无幂律 ⇒ **另一类机制**（非"施加次数"）；注意 `j_av≈−0.3` 的子群与 D 类的恒定乘子形态**相邻**（512250.SH 同时出现在 D 类与 `k_int=0` 组） |
| 建议方法 | 逐码日内逐 bar 取证（close vs amount/vol 的 bar 级关系）+ 因子历史（是否缺锚/多锚） |
| 归属 | Trae 主修单后续批次（用户裁定③） |

### W-3：V-3 第二层定性（512250.SH）

| 项 | 内容 |
|---|---|
| 已知 | f 恒 **1.0197**（±0.03%）、无幂律（R²=0.012）、该码无任何正常类（`A=B=C=0`） |
| 待查 | ①日内逐 bar 是否全天恒定 1.97%；②是否涉份额拆分/合并或基金合同变更；③日线表是否同异常 |
| 归属 | Trae 主修单后续批次（用户裁定③） |

### W-4（登记）：V-4 NO_ADJ → 数据域

| 项 | 内容 |
|---|---|
| 结论 | **整码级因子覆盖缺失**：167 码在三张因子表 **0/167**，在 `etf_basic` **亦 0/167** |
| 性质 | **因子/基础信息通道覆盖问题**，独立于主修（分钟表施加次数）缺陷 ⇒ **不可用回填解决** |
| 处置 | 转**数据域新发现登记**；列入**下次云端交互议题**或 Trae 后续批（用户裁定④） |
| 我方已登记 | `QuantStudio/docs/pipeline-tech-debt.md` 新增该条目（见 §四） |

---

## 三、移交边界

- ❌ 本会话**不实施**上述 W-1~W-4（按裁定并入 Trae 主修单后续批次 / 数据域）；
- ❌ 本会话**不立新追单**（qfq 域统一管理）；
- ❌ 本会话**不回填**任何数据（D-4 口径）；
- ✅ 本会话已完成：四件定性 + 勘误块 + 全部只读取证脚本（可复现）。

---

## 四、数据域登记（本会话已落）

`QuantStudio/docs/pipeline-tech-debt.md` 新增条目：
**「ETF 因子/基础信息通道覆盖缺失（NO_ADJ 族）」** —— 含 167 码清单指针、三表命中 0/167 实测、
`etf_basic` 0/167 交叉验证、双峰形态（P50=3 / TOP≈300+）、与主修缺陷的异族判定、待查项（缺席原因 /
码池口径 / 时效性 / 下游数据可用性风险）。