# 勘误回填 · 方案 §九 V-1 `r_max` 区间修正（已批准）

- **日期**：2026-09-24
- **批准**：用户 2026-09-24 裁定①（V-1 的 `r_max` 修正 1.0297~2.0207 **批准回填方案 §九**，随下一 docs 批）
- **目标文件**：`D:\miniQMT策略实盘\trading-battle-back\docs\minutes-qfq-apply-count-repair-plan-20260924.md` §九 V-1 行
- **本次未直接改写目标文件的原因（守法如实）**：该文件在 `trading-battle-back` 仓中为
  **未跟踪状态（`git status` 显示 `??`）**，属**其他会话（Trae 域）尚未提交的工作**。
  按「共享核心文件提交纪律」与「写前快照纪律」，**不得直接改写他人未提交文件**
  ⇒ 本会话交付**可直接粘贴的勘误块**，由该文件归属方并入其下一提交。

---

## 一、勘误块（可直接替换 §九 V-1 行）

**原文（L300）**：

```markdown
| V-1 | **X 类**（非整数 j） | 4,965 code-day / ~33 码 | `r_max` 1.03~1.115，j 非整数 | 疑 **QFQ 旧锚**（历史基准未随最新因子迁移）或局部异常；需第二层定性 |
```

**修正后（建议替换为）**：

```markdown
| V-1 | **X 类**（非整数 j） | 4,965 code-day / 33 码 | `r_max` **1.0297~2.0207**（P5/P50/P95 = 1.0297 / 1.0514 / 2.0207；含 588890.SH `r_max=3.0008`），j 非整数 | **已定性（2026-09-24）**：非「分数次施加」，而是「**整数次施加 + 全局锚偏移 ≈0.87%**」复合（幂律 `ln f = k·ln r + c`，k 距最近整数中位 0.0105，R² 中位 0.9917，c 中位 +0.0129，`c/k_int` 两 k 组同为 0.9913~0.9915；校正 c 后 j 回整数 {1:1,058, 2:2,671, 3:1}，95.5% 落 ±0.05）；**仍未竟**：11 码 `k_int=0`（无幂律、R²≈0）待第二层；锚偏移来源端未定位。详见 `QuantStudio/docs/evidence/backlog-v1-x-class-verdict-20260924.md` |
```

---

## 二、修正依据（数字出处）

| 指标 | 实测值 | 出处 |
|---|---|---|
| `r_max` 分位（X 码 33 只） | P5 = **1.0297** · P50 = **1.0514** · P95 = **2.0207** | `probe_x_class_third_source.py`（codes.csv `X>0` 子集） |
| 最大值 | `588890.SH` = **3.0008** | 同上（X 码 r_max 逐一列出） |
| 7 只 `r_max ≈ 2/3` 的码 | 159325 1.9990 / 159560 1.9991 / 588290 3.0000 / 588920 2.0000 / 589020 1.9992 / 588810 2.0000 / 588890 3.0008 | 同上 |

**原区间 1.03~1.115 的偏差性质**：低估了 X 码的 `r_max` **上界**（实际含 2.0/3.0 量级的整倍率事件码）
——上界低估会让人误以为 X 类只发生在「小因子」语境，**掩盖与整倍率事件的相关性**（该相关性在 V-2 中亦被独立观测到）。

---

## 三、同批建议一并勘误的两处（供归属方参考，非批准范围）

| 位置 | 现状 | 建议 | 依据 |
|---|---|---|---|
| §九 V-2 行 | 「2,184 = B→A 2,162 + A→C 22」 | 补注：**逐行明细未落工件**（`detail.csv` 保存的是**日线锚优先裁定后**的 `class_av`，B→A 仅存 1 行）⇒ 若需逐行定性须由分类引擎补 `class_av_raw` 版工件（用户裁定②已批准补该工件） | `backlog-v2-r-class-verdict-20260924.md` |
| §九 V-4 行 | 「NO_ADJ ... 因子表缺口，属另一缺陷族」 | 补注**实测规模与性质**：167 码 / 4,441 code-day；**三张因子表命中 0/167**（非日期缺漏，是**整码级覆盖缺失**）；**且这 167 码在 `etf_basic` 亦 0/167** ⇒ 属**因子/基础信息通道覆盖问题**（正在转数据域登记） | `backlog-v4-noadj-verdict-20260924.md` |

> 说明：上述两处属「建议」，是否一并勘误由方案归属方与调度裁定；本会话不越界改写。

---

## 四、归属与后续

| 项 | 说明 |
|---|---|
| 回填责任方 | 方案件归属方（Trae 域，该文件当前未提交） |
| 时机 | 随其下一 docs 批（用户裁定①） |
| 本会话已完成 | 勘误块交付 + 数字出处 + 依据件指针 |
| 若归属方希望本会话直接改 | 需先把该文件纳入版本控制（提交或明确授权），届时本会话可按「共享核心文件提交纪律」改（建回退点 + 精确清单） |