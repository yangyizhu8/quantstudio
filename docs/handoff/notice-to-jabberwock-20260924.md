# 通知件 N-20260924-02（jabberwock）：取数周期 IndexError 停摆修复

- 状态：**已出件（2026-09-24）**——推送已落地（远程 HEAD = **`1ae083f`**，三方核对 local = origin/main = 双远程一致）
- 下发：**用户转发**（客户确认通道 = 用户转发；回执登记见 `docs/handoff/customer-notice-registry.md`）
- 对应客户：**jabberwock**（部署 `D:\hasym\PycharmProjects\QuantStudio`）
- 缺陷链与修复：`docs/jabberwock-ckey-empty-shards-fix-design.md`（快审通过 `c44c81f`）

---

**【修复完成】取数周期异常停摆已修复，请按 4 步更新（约 5 分钟）**

**问题现象**：采集进程偶发报 `IndexError`（`[qfq_orch] 周期异常停摆重锚环`）——某个取数周期中断，之后反复重试仍停在同一步。

**原因**：本地缓存索引里记着一批已下载的分片，但**这些分片文件已被清理/过期**（或跨版本残留）。程序按索引去读文件时没读到，回退重新下载；若该时间窗**确实没有产出**，旧版本会在这里直接抛错中断整个周期。

**重要（请知悉）**：
- 此问题**不会损坏数据**——属于「读取环节的防御缺失」，已入库数据完整可用；
- **重启程序仍会撞上**（不是重启能解决的）；
- 更新到修复版本后即可解决。

**更新 4 步**（先**停止采集程序**；PowerShell 中执行）：

```powershell
cd D:\hasym\PycharmProjects\QuantStudio
git pull origin main
git log --oneline -1        # 应显示 9ad9cf2 或更新（含本次及后续多项修复）
# 然后重新启动采集程序，观察一个完整采集周期
```

**更新后核对 3 项**
1. 采集周期**正常跑完**，不再出现「停摆/重锚环」；
2. 日志中**不再出现 `IndexError`**；
3. 若日志出现 `export_cache 重取无 shard（ckey=…）：本批降级为空、不写 manifest，周期继续` —— 这是**修复后的正常降级**（该窗口确无产出，程序继续跑并在下个周期自然重试），**不是故障**；如方便可把该行回传我们备案。

**另外请协助一项排查（静默终止问题）**

您上次反馈的增量拉取最后无声退出问题，我们已定位三个候选原因，需要您帮忙提供三件材料来最终确定：

1. 在您的项目目录执行 `git log --oneline -1`，把输出发给我们（确认您当前运行的代码版本）；
2. 在日志中找到最后一次 `CHECKPOINT` 相关的行，把**前后各 30 行**发给我们；
3. 打开 Windows 事件查看器（`eventvwr`），在「Windows 日志 → 应用程序」中找到 **2026-09-25 凌晨 04:00–04:15** 时段的**错误**级别事件，截图或复制内容发给我们。

**若仍未解决**
重启后仍出现 `IndexError` 或其他异常 → 请把**日志片段**（含报错前后各 20 行）与 `git log --oneline -1` 的输出发我们；**请保留现场**（不要删除缓存目录与日志），我们会立即跟进。

---

## 内部备注（不下发）

| 项 | 内容 |
|---|---|
| 编号 | **N-20260924-02**（接续 N-20260924-01） |
| 下发前置 | **修复推送落地**（推送后填入 HEAD SHA 再下发） |
| 答复口径来源 | ZCode 2026-09-24 裁定（现象/不损数据/重启仍撞/git pull 即解） |
| 回报路由 | R1 常规确认 / R2 需现场证据 / R3 立即升级（见 `docs/handoff/customer-notice-registry.md`） |
| 归属线 | 客户运维会话 1（jabberwock） |
