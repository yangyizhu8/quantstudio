# 致客户C（macOS） · 写锁残留停更问题修复完成通知（定稿，可直接发送）

- 定稿：2026-09-18｜对应版本：`6b8fde1`（修复）+ `7192017`（使用说明修正）
- 客户C 安装目录（macOS）：`/Volumes/ssd/quantstudio`
- **内部信息已剔除**（残留锁持有者 pid 与陈锁年龄不随通知下发）
- 发送状态：待用户/对接人下发 → 收到确认后进六步⑥案例归档

---

**【修复完成】数据停更问题已修复，请按 4 步更新（约 5 分钟）**

您好，您此前反馈的「数据停止更新」问题已完成修复，现说明原因、修复内容与更新步骤。

**一、问题原因**

采集程序在写库过程中异常结束了（常见于关机、重启或强制结束进程），把一把「写锁」留在了数据目录里没有释放。
旧版本对这种**残留锁**只会告警、不会自动清理，于是之后每次写库都会在等待 30 秒后失败——数据就停在那里不再更新。
**这不是数据库损坏，您已有的数据都是完整可用的。**

**二、本次修复内容**

1. **残留锁自愈（核心）**：系统在获取写锁前会先判断「持有锁的那个进程是否还存在」——
   - 进程**已不存在**（您遇到的正是这种情况）→ 安全回收残留锁并恢复写入，同时记入审计日志与告警；
   - 进程**仍在运行**（例如长任务）→ **一律不回收**，避免两个写入者同时写库造成数据风险。
2. **宽文本类表拉取修复**：修复了 7 张宽文本表（AI 研报快照、研报文本、新闻等）拉取时的报错，此前这些表必然失败。
3. **使用说明同步修正**：原说明写「次日自动补齐」——该表述在本次场景下不成立，已改为明确区分「锁残留」情形，并给出四步处置方法。

**三、更新操作（4 步，约 5 分钟）**

> 更新前请先**停止采集程序**（不要在采集中途更新）。以下命令在「终端」中逐行执行。

```bash
# 第 1 步：进入安装目录
cd /Volumes/ssd/quantstudio

# 第 2 步：拉取最新修复（沿用原有仓库访问方式，无需重新配置密钥）
git pull

# 第 3 步：确认已更新到最新版本（应看到 7192017 或更新的提交）
git log --oneline -1

# 第 4 步：重新启动采集程序，观察一个周期
```

> 若您此前不是用 `git` 克隆安装的（例如拿的是压缩包），请联系我们获取「只覆盖代码、不动数据」的更新方式——**切勿删除或替换 `data/` 目录**，也不要用新克隆的空目录直接启动（那会把数据库一起换空）。

**四、更新后的验证清单**

| # | 检查项 | 正常表现 |
|---|---|---|
| 1 | 启动日志 | **不再出现**「写锁被持有」 |
| 2 | `data/snapshots/` 目录 | 没有残留的 `.write_lock`；若出现 `write_lock_reclaim.log`，**1 条属正常**（= 首次启动回收了历史遗留锁，是审计留痕） |
| 3 | 一个采集周期后 | 任务出现 `raw=... written=...` 的成功行，数据恢复更新 |
| 4 | 宽文本类表（AI 研报快照 / 研报文本 / 新闻等） | 不再出现 `_config` 报错，任务正常完成 |
| 5 | 若仍出现「写锁被持有」 | 请把日志回传我们（并按下文四步处置，不要直接删除锁文件） |

**四之补：若仍未恢复，请这样做（重要）**

- 若「写锁被持有」在**重启后超过 10 分钟**仍持续出现 → **请把日志发我们，并保持
  `data/snapshots/.write_lock` 原样（不要删除、不要改名）**，我们据此判断属于哪种情形；
- 日志里会写明判定结论，便于您区分：
  - 「**持有者进程仍存活**」→ 该进程可能卡住，请把 `ps -p <该 pid>` 的输出发我们；
  - 「**持有者进程已不存在**」但仍未恢复 → 属异常路径，请立即联系我们；
- 请勿在采集运行中删除锁文件——那可能让两个写入者同时写库。如确需清理，
  请先停止采集程序，并按「锁残留处置」四步执行。

**五、更新后的预期行为变化（如实说明）**

1. **正常情况**：多个任务竞争时仍是「谁先拿到谁写」，等待时间上限 30 秒，不改变您的数据口径与使用方式；
2. **异常情况（进程被强制结束/断电）**：锁**不会**立即释放，而是**在残留锁心跳过期（约 10 分钟）后自动回收**。
   即：最坏表现是**最多约 10 分钟的写入等待**，而不再是**永久停更**；
3. 我们**不宣称**已解决所有锁相关问题：进程被强杀/断电的「瞬时」释放需要另一项底层改造（已在计划内）。
   本次确保的是：**进程确实已经终止后，系统能自己恢复**——这正是您这次遇到的情形。

**六、请回传给我们**

- 第 3 步命令的输出（版本号）；
- 重启后一个采集周期的日志（含第一条 `written=...` 成功行）；
- 若第 2 项出现 `write_lock_reclaim.log`，请把该文件一并发我们（我们据此确认自愈真实生效）。

---

**附：一页速查（可留存，macOS 命令）**

```
问题特征：日志反复出现「写锁被持有」→ 数据停更（不会自动恢复）
四步处置（macOS）：
  ① 留证：cp data/snapshots/.write_lock ~/Desktop/write_lock_$(date +%Y%m%d).json
  ② 确认进程不存在：ps -p <pid>        # 无输出 = 已不存在；有输出 → 停手联系我们
  ③ 改名（不要删除）：mv data/snapshots/.write_lock data/snapshots/.write_lock.stale.bak_$(date +%Y%m%d)
  ④ 重启采集程序并观察一轮
  （③ 的「改名」仅适用于**更新到 7192017 之前的旧版本**应急；更新后请保持锁文件原样、回传日志即可）
新版行为：进程确实终止后，系统会在约 10 分钟内自动回收残留锁（无需人工）
超过 10 分钟仍未恢复：保持锁文件原样 → 回传日志 + ps 输出 → 联系我们
切勿在采集运行中删除锁文件（可能导致双写入者）
```
