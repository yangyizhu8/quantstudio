# QuantStudio 项目工作规则（必须遵守）

## 【铁律】框架层改动六步流水线：方案 → 审计 → 实施 → 验收 → 用户确认 → 双仓库推送

### 适用范围
凡涉及以下**本地回测框架层面**的修复/改动，一律适用本规则：

- 框架 API 函数（如取数 API、策略工具箱注入 API 等）
- 数据适配层（数据读取、复权处理、字段映射等）
- 框架生命周期函数（initialize / before_trading / handle_data / after_trading 等）
- 其他属于回测框架内核层面的修复（编译器契约、校验器、QFQ/复权管线、数据巡检等框架组件同理）

### 必须执行的流程（六步流水线，任何一步缺失即违规）

1. **先给方案**：任何框架层改动，必须先产出书面方案（设计文档，如 `docs/*-design.md`），
   至少包含：问题定义、改动范围、影响面、验收标准、回退条件。**禁止未给方案直接改代码**。
2. **审计方案**：方案提交独立审核（ZCode 等审核方）审计通过后，才能进入实施。
   审计未通过 → 修订方案重新审计，**禁止带病实施**。
3. **实施**：按审计通过的方案实施改动，**绝不擅自**提交或推送到 GitHub。
4. **验收**：实施完成后必须验收（单元测试全绿 + 回归/黄金结果对比 + 差异逐项归因等，
   按方案验收标准执行），验收结论写入证据文档（如 `docs/evidence/*.md`）。
5. **用户确认**：向用户完整汇报（改动内容 + 验收证据），**经过用户明确同意确认之后**，
   才能执行同步提交。
6. **双仓库推送**：确认后 `git push origin`（多 push URL 双仓库同步，2026-08-12 起），
   推送后核对两个远程仓库均一致：
   - https://github.com/yangyizhu8/quantstudio-plus
   - https://github.com/yangyizhu8/quantstudio

### 同步内容必须完整

- 相关框架代码文件的更新；
- `README.md` 中涉及这些修复的相关内容（策略工具箱、提示词工程等章节）；
- 引用文档中涉及的相关内容，至少包括：
  - `docs/strategy_toolbox.md`（策略工具箱）
  - `docs/prompt_engineering.md`（提示词工程）
- 即：README 正文 + 上述引用文档中所有涉及该修复的表述，都要同步更新，确保 GitHub 项目与本地实时一致。

### 禁止事项

- 禁止未给方案、或方案未审计通过即开始实施。
- 禁止在未获用户确认前，将框架层修复推送/提交到 GitHub（含任何代理顺带提交）。
- 禁止跳过六步流水线中的任何一步（方案/审计/实施/验收/确认/推送）。
- 禁止只更新代码而遗漏 README 及 docs 引用文档的同步更新。

> 用户原话要求：任何涉及框架层的改动，都需要先给方案，再审计方案，最后实施，实施后验收完成，用户确认后再走双仓库推送。养成习惯，写死到记忆中，确保以后不会忘记执行，从而确保 GitHub 上的项目保持实时同步。

## 【铁律】性能优化不得改变回测引擎行为与逻辑

### 适用范围

当用户提供 CodeBuddy 或其他智能体给出的性能分析、内存优化、速度优化、缓存优化、SQL 优化、批量取数优化、并发优化等反馈，并要求审核或给出回复意见时，一律以本铁律为最高判断标准。

### 核心原则

任何被定义为“性能优化”的改动，必须是**语义完全等价的内部实现优化**，不得改变当前项目的回测引擎行为、策略逻辑、数据语义或可观察结果，包括但不限于：

- 不改变框架公共/注入 API 的函数名、签名、默认值、返回类型、返回字段、列顺序、索引、dtype、代码格式、空值行为、异常行为和兼容行为；
- 不改变行情取数范围、日期边界、PIT 规则、复权口径、字段映射、数据源优先级或 fallback 顺序；
- 不改变 initialize / before_trading_start / handle_data / after_trading_end 等生命周期的调用时机、顺序、次数和上下文；
- 不改变撮合价格、订单执行、成交、费用、滑点、T+1/T+0、持仓、现金、公司行为、涨跌停、停牌或退市处理；
- 不改变策略信号、选股结果、调仓日期、订单序列、成交序列、每日持仓、每日净值和最终回测指标；
- 不以“性能优化”为名混入正确性修复、API 重构、数据契约调整、字段删除、逻辑简化或其他行为变更。

如果某项建议可能改变任何上述行为，则不得将其作为纯性能优化批准；必须明确重新分类为“框架行为/正确性变更”，单独说明影响、风险和迁移方案，并遵守本文件中的框架层修复确认与 GitHub 同步流程。

### 审核 CodeBuddy 性能反馈时的强制要求

1. 必须先核对真实生产调用链，确认被优化对象确实被当前运行路径消费，不能只根据函数名、注释、测试辅助方法或孤立 SQL 推断。
2. 优先选择最小、可逆、语义等价的实现；不得为了追求理论性能收益扩大修改范围。
3. 对未被生产路径使用的缓存或计算，优先停止无效分配/调用，不得顺带修改活跃查询、日期语义、字段契约或回测逻辑。
4. 性能修复与正确性修复、功能增强、死代码删除、批量 API 重构等必须拆分；未经明确授权不得捆绑实施。
5. 给用户的回复意见中必须明确列出：允许改动范围、禁止改动范围、潜在行为风险、等价性验收标准和回退条件。
6. 不得声称“保证不改变逻辑”而不提供验证证据；最终结论必须以修复前后回归与黄金结果对比为依据。

### 纯性能优化的强制验收

性能优化完成后，除性能/内存指标外，必须证明修复前后行为等价，至少包括：

- 相关 API 的返回值、结构、字段、索引、dtype、空值和异常行为一致；
- 代表性历史区间以及股票、ETF、指数/fallback 等数据场景一致；
- fq、include、count、fields 等相关参数组合行为一致；
- 代表性策略的信号、选股、订单、成交、费用、持仓、现金和每日净值逐项一致；
- 最终收益、回撤、交易次数等回测结果一致；
- 生命周期顺序、PIT、复权、撮合和交易规则一致；
- 仅允许已有测试契约明确规定的浮点容差，禁止为使测试通过而自行放宽容差。

任何可观察结果不一致，都必须视为性能优化验收失败：立即停止该方案并回退，不得以“差异很小”或“结果更合理”为理由接受。

### 默认回复立场

今后用户发送 CodeBuddy 性能优化反馈并要求给出回复意见时，默认回复立场为：

> 仅接受不改变当前回测引擎、策略逻辑、数据语义和回测结果的纯性能优化。先核对生产调用链，再限定最小改动范围，并以修复前后黄金结果逐项一致作为通过条件。任何可能改变行为的内容必须剥离并重新按框架行为变更审核。

本铁律长期有效，除非用户以后明确要求修改或撤销。

## 【铁律】代码层"写前快照"纪律（多会话共享工作区防护）

背景（2026-08-17 事故）：多会话共享同一 git 工作区 + 并行未提交改动 + 批量破坏性 git 操作
（checkout/reset/revert），曾覆盖另一会话的未提交工作（后经 dangling blob 找回）。
本纪律与数据层快照原则同构，防止同类事故重演。

1. **任何批量/破坏性 git 操作（checkout -- / reset / revert / clean / stash pop 冲突覆盖）前，
   必须先建零副作用回退点**：`git stash create -u -m "baseline-<时间戳>"`（只建对象不动工作区），
   **随即 `git stash store <hash>` 持久化**（create 产出的对象无引用、可被 `git gc` 清理；
   store 固定进 stash 引用后才防丢，2026-09-04 v10 线实操确认纳入），
   将返回的 commit hash 记入会话记录或登记表；误操作后 `git reset --hard <hash>` 找回。
2. **操作前核对目标文件状态**：`git status --porcelain` 确认待回退文件是否含**其他会话的
   未提交改动**（非本会话创建的 M 文件一律视为他人工作，禁止盲回退）。
3. **每轮会话开始时记录工作区基线**：`git status --porcelain` + `git diff --stat`
   落盘到 `docs/handoff/`（轻量基线，供溯源）。
4. **回退优先用逐文件定向手段**（文件级 cp 备份/恢复、Edit 工具），
   批量 git 回退是最后手段且必须在回退点建立之后。

## 【铁律】策略生成默认通过 skill 开发，仅本地 QuantStudio

1. **默认行为**：用户给出策略开发提示词时，默认通过 `quantstudio-strategy-compiler` skill 开发，
   生成 **QuantStudio 本地零依赖策略**并落盘 `quantstudio/backtest/strategies/`。
2. **skill 默认 QuantStudio-only**（不生成 PTrade）。PTrade 转换由 PyQt 的"转 PTrade"tab
   或终端命令（`qs-compile import`）承接，不在策略开发期承担。
3. **ETF 策略默认用 `get_etf_list_local` 做 PIT 动态池**。转 PTrade 时由转换功能
   根据用户确认的回测起始日固化静态池（`ETF_POOL_STATIC`，见 07-ETF动态池固化补充规格）。
4. **用户可明确要求"跳过 skill 直接手写"**来绕过完整流程（应对简单策略）。
5. **历史教训**（2026-07-26）：旧版 skill 产出的 PTrade 代码在真实 IQEngine 报
   `NameError: name 'set_backtest' is not defined`——本地自创 API 混入 PTrade 目标。
   新版分工（skill 只产出本地代码 + 转换管线承接 PTrade）从源头消除该问题；
   旧产出物 `ptrade/tech_etf_mvo_rotation_ptrade.py` 已被真实平台证伪，视为失效。

## 【铁律】MCP 全数据源替代任务进度报告必须实时更新

### 适用范围
QuantStudio MCP 全数据源替代项目（含云端数据基础设施、同步管线、MCP server、客户端接入等全部子阶段）。
本铁律长期有效，贯穿项目从启动到正式切换的完整生命周期，直至项目闭环。

### 进度报告唯一权威档案
- **位置**：`D:\miniQMT策略实盘\私募工作文件\QuantStudio-MCP全数据源替代任务文件\实时进度报告.md`
- **性质**：该报告是本项目**唯一权威进度档案**，记录已完成、待需完成、待办（技术债/合规/收尾）的全部状态。
- **用途**：作为项目指导思想与闭环审核依据，防止阶段推进中遗漏重要的收尾工作（如技术债、合规门禁、凭据管理、文档同步）。

### 必须执行的更新流程
1. **触发时机**：CodeBuddy / workbuddy 每推进一个阶段（或完成一个工作包、修复一组问题），**ZCode 审核通过后**，必须立即更新本进度报告。
2. **更新内容**至少包括：
   - 将该阶段从"待需完成"移入"已完成"，附关键证据（SHA、行数、测试结果）；
   - 刷新"待需完成"列表（依赖关系、下一步）；
   - 记录新发现的技术债/风险/合规问题到"待办"清单；
   - 更新"阶段进度总览"对照表；
   - 在"变更记录"追加一行（版本、日期、内容）。
3. **禁止事项**：
   - 禁止跳过审核直接更新报告（必须先审核通过再更新）；
   - 禁止只更新代码/配置而遗漏报告更新（代码与报告必须同步）；
   - 禁止用模糊表述（如"基本完成"）代替具体证据（行数、SHA、测试通过数）；
   - 禁止删除已记录的技术债/合规问题（除非已确凿解决，解决时标注解决方式与日期）。

### 闭环审核要求
项目宣布"完成"前，必须对照本进度报告逐项核查：
- 所有"已完成"项有证据、所有"待办"项已解决或有明确处置、所有技术债已立项或接受、C0 合规已决、凭据已妥善管理、文档已同步。
任何未闭环项不得宣布完成。

> 用户原话要求：养成习惯，写死到记忆中，确保项目完成后可对照审核，不遗漏收尾工作。

## 【铁律】Rust 开发：写 .rs 前必须检索 rust教程 校准语法

1. **强制前置动作**：编写/修改任何 `.rs` 文件之前，必须先检索
   `D:\miniQMT策略实盘\rust教程\` 目录中的对应编号文件（如 `02.Rust rustup.md`、
   `24.Rust Result 类型.md`、`33.Rust match.md` 等，目前共 104 个编号教程文件），
   按主题（变量/类型/函数/控制流/错误处理/生命周期等）找到对应编号文件校准语法后再动手。
2. **适用范围**：所有 Rust 代码开发任务，无论代码落在哪个工作区/仓库。
3. **核对内容**：语法写法、API 用法、惯用模式（Idiomatic Rust）均以教程文件为准；
   不确定的语法不得凭印象编写，先检索确认。
4. 本铁律长期有效，除非用户明确要求修改或撤销。

> 用户原话要求：rust开发铁律——写 .rs 前必须检索 D:\miniQMT策略实盘\rust教程\ 对应编号文件校准语法。

## 【铁律】策略生成与转换全链路修复：仅限框架层·纯增益·不遗留策略层

本铁律约束 QuantStudio 项目中「策略代码生成 skill → 本地回测策略 → 转换管线 → PTrade 策略」全链路上遇到的**所有阻断点或问题**的修复工作（含框架引擎、注入 API、数据访问层、数据源、策略生成 skill、转换管线、校验器、保真配置等通用框架层面）。**所需修复必须全部落实于通用框架层，禁止修改任何具体策略源码强行修复，且不得将问题遗留至具体策略层。**

1. **修复范围仅限框架层，策略源码零改动**：所有阻断点/问题的修复落点必须是框架引擎、API 函数、数据访问层、数据源管线、策略生成 skill、转换管线、校验器、保真配置等通用层面。**禁止**通过修改某个具体策略的选股逻辑、止损阈值、调仓节奏、仓位权重、代码路径来强行修复——具体策略源码（含 skill 生成的本地策略与转换产物）必须保持**零改动**；修复通过重新生成/重新转换（重转）产物生效。
2. **通用手段（不遗留策略层）**：框架层可通过注入/包装 API（如持仓视图归一、订单语义修复）、数据访问层容错（如撮合价窗口匹配）、转换管线模板与门控注入、保真开关（默认关）、审计行/诊断日志、参数链（CLI 旗标）等通用手段解决，禁止把问题下推给具体策略代码。
3. **通用性与鲁棒性**：修复必须具备通用性（多策略可覆盖，不得只对单一策略生效）与鲁棒性；若某修复只对单一策略生效而无法覆盖其他策略，说明落点不是通用框架层。
4. **纯增益修复**：修复必须为纯增益——不得引入新问题、不得破坏现有正常功能、不得出现「修一送一」。验收必须证明修复前后既有功能零衰减（相关测试套件 + 既有功能回归全绿）。
5. **通用性验收（多策略横验证）**：涉及策略产物时以多策略横验证为验收门槛——6 策略（CANSLIM / fall_reversal / tech_etf_mvo_rotation / vol_regime_mom_rev / weekly_smallcap_growth / 周频小市值成长动量（三层止损））重转 api_portability 全 PASS + 相关测试套件全绿 + 既有功能回归全绿。
6. **改动层级判定**：判定修复层面的标准——(a) 改动文件是否为框架模块（引擎/数据访问/管线/校验器/模板/skill），(b) 策略 `.py` 文件是否被编辑（禁止），(c) 修复是否通过重转/引擎容错/保真开关生效（而非改策略）。
7. **配套纪律（六步流水线协同）**：框架修复同样必须走 方案→审计→实施→验收→用户确认→双仓库推送；与「框架层改动六步流水线」协同执行；与「性能优化不得改变引擎行为」界定一致（正确性修复不等于性能优化）；与「框架问题立即解决」衔接（发现阻断点默认立即推进修复，禁止以登记挂账替代）；与「策略生成默认通过 skill 开发，仅本地 QuantStudio」衔接（skill 产出本地代码、转换管线承接 PTrade，全链路的阻断点修复即本铁律范围）。
8. **例外与登记**：确需策略层配合的场景（如未来经用户批准修改策略模板/新策略生成标准）必须显式立项、走六步流水线、并在引用文档更新后由用户确认——禁止默认以策略层改动作修复手段。
9. 本铁律长期有效，**跨会话、跨项目、跨智能体（dsh/CodeBuddy/workbuddy/ZCode 等）必须共同遵守**，除非用户明确要求修改或撤销。

> 用户原话要求：QuantStudio 项目中策略代码生成 skill 生成本地回测策略、以及转换管线转换为 PTrade 策略时遇到的所有阻断点或问题，所需修复必须全部落实于通用框架层（框架引擎/数据访问层/转换管线/校验器/保真配置/策略生成 skill）；禁止修改任何具体策略源码强行修复；修复须具备通用性与鲁棒性、须为纯增益（不引入新问题、不破坏现有正常功能、不修一送一）；修复完成必须执行六步流水线协同；作为跨会话和项目必须遵守的铁律。

## 【铁律】框架问题立即解决：禁止"登记挂账"替代修复

发现任何框架层缺陷（引擎/注入 API/数据适配/转换管线/校验器/QFQ 管线等，适用范围同「框架层改动六步流水线」）时：

1. **禁止仅登记挂账**：缺陷一经发现，登记（issue_registry / 进度报告 / TODO）只是追溯记录，**绝不构成已处置**——不得把问题写成 pending 债务后就搁置。登记条目的状态必须反映真实进度：发现即计入待修，但要在同一工作周期内启动并推进修复。
2. **默认必须立即解决**：发现框架缺陷 → 立即定位根因（能复现先复现，禁止基于未证实的归因动手）→ 进入六步流水线（方案→审计→实施→验收→用户确认→双仓库推送）。"需要走流程"不等于"可以挂账"；方案与修复当轮推进，验收证据随修复落地。
3. **根因未证实不得修**：若现象可被多种解释覆盖（如拒单可能是引擎核算价来源、也可能是策略参数/数据问题），必须先做最小复现/插桩取证，**证据确凿后才写方案改代码**——严禁按猜测的"框架债"方向提前动手或提前登记错误根因。
4. **唯一例外**：修复需要外部依赖（真实平台/数据源/其他团队）或用户明确裁定"暂缓"时，才允许登记并标注 `BLOCKED(外部依赖/用户裁定)` + 解除条件；依赖解除后立即恢复修复流程。
5. **关单标准**：框架缺陷只能以"修复 + 回归证据"关单，不允许以"已登记"关单。
6. 与「框架层改动六步流水线」协同执行；与「性能优化不得改变引擎行为」「策略生成与转换全链路修复仅限框架层」相互独立、兼管。
7. 本铁律长期有效，跨会话、跨智能体（dsh/CodeBuddy/workbuddy/ZCode 等）必须共同遵守，除非用户明确要求修改或撤销。

> 用户原话要求：不要登记框架债，框架问题必须立即解决，请写入项目铁律，而且需要跨会话和其他智能体都遵守。

## 【铁律】跨任务总调度常设职能与三件套实时更新纪律（2026-08-30 修订）

### 总调度常设职能（双项目分层调度）
1. **跨任务总调度 = 主线会话**（三件套唯一维护/跨线冲突终审/共享层修复排程/分割点管理/
   转发守护履行），调度+审核+监控三位一体不变。
2. **QuantStudio-trading 实盘线调度 = 交易线审计会话**（M 阶段排程提案/同步门复核/
   交易线六步审计/冲突识别+建议上报/结构化周报）。
3. **执行会话单线汇报**（执行→线调度→总调度，禁双头平行汇报、禁越级派活）；
   主项目回测执行由总调度既有执行会话群承担。
4. 共享层只修于主仓（六步流水线）；副本共享层零本地改动（AGENTS.md 跟随制）；
   增量层（trading-runtime）只存副本永不回流主仓。
5. 不可逆动作（推送/移交/M5/M6）保留用户确认闸门。
6. 任一调度会话不可用须落盘完整交接文档。
2. 任何项目（不限于 QuantStudio）的推进都按既定工作流与节奏执行：
   统一排程（串行/并行无冲突）→ 各归属任务会话执行 → 执行反馈经总调度审核（实测独采）
   → 时间点守护自动核验/提醒 → 需人工的报用户（时间点+操作+归属任务+负责会话）。
3. 跨任务总调度是确保项目各任务稳定有序推进、不遗漏任何节点的关键，不得弱化或跳过。

### 三件套实时更新纪律
1. **跨任务调度总日历、彻底闭环清单、技术债清单必须随任务完成情况与依赖关系实时更新**——
   任何任务的完成/新增/变更都要同步反映到三件套，禁止陈旧状态留存。
2. **依赖驱动的守护联动（核心）**：凡新增或更新的任务，若其执行依赖总日历上某个任务作为
   前置依赖（如"A 需等 B 完成后才能启动"），必须**立即在该前置任务的完成时点增加或更新守护提醒**，
   确保前置完成信号准时送达依赖方会话（经用户转发或信箱），不因等待而空转、不因遗忘而断链。
3. 守护提示词遵守既有纪律：文本避开 guard 白名单关键词（拆分/代称）；跨日守护必须核对目标
   日期星期；守护到点执行前必须核对现场状态（防误触/误杀）。

## 【铁律】共享核心文件提交纪律（2026-09-03 追加·多会话写前快照案例固化）

1. 涉及 `quantstudio/strategy_compiler/source_import.py`、`quantstudio/backtest/ptrade_api.py`、
   `quantstudio/backtest/backtest_engine.py` 等**共享核心文件**的改动，提交前必须：
   ① `git stash create -u -m "baseline-<时间戳>"` 建立零副作用回退点（只建对象不动工作区），
      **随即 `git stash store <hash>` 持久化**（防未引用对象被 GC 清理，与铁律③写前快照细则同款）；
   ② `git status --porcelain` 核对待提交**精确文件清单**只含本会话改动；
   ③ **提交信息自查不含他人改动内容**（2026-09-03 事故：本会话 source_import 吸收规则
   改动被其他会话整体提交卷入 commit 0321364 推送，事后追认——禁止重演）。
2. 禁止 `git add -A` / `git add .` 全量提交共享工作区（他人未提交改动会一并卷入）；
   一律用精确文件清单 add。
3. 批提交/推送后必须核对**双远程 HEAD 逐位一致**（多 push URL 双仓库，2026-08-12 起）。
4. 涉及共享核心文件的代码先行会话，若检测到他人未提交改动叠加（git status 该文件已 M），
   须在提交信息与设计文档中显式记录叠加事实，不得静默带过。
